# 通用降级（对话触发）
1. 注入 skills/metis/SKILL.md 全文为纪律。
2. 全部动作经引擎 CLI：init/status/plan/tasks/exec/advance/artifacts/deliver/verify。
3. MCP 可用的 Agent：经 mcp/metis-server.py（stdio）或网关（HTTP+令牌）。
4. 检查点纪律不因降级而豁免。
