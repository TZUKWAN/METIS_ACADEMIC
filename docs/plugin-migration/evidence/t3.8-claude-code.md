# T3.8 Claude Code 本机注册断言（2026-09-26）

1. 注册：`claude mcp add metis -s local -- python <plugin>/mcp/metis-server.py --workspace <repo>`
   → "Added stdio MCP server metis ... to local config"
2. 连接：`claude mcp list` → `metis: python ... - ✔ Connected`
3. 真实工具调用：`claude -p "使用 metis MCP 的 echo 工具..." --allowedTools mcp__metis__echo`
   → 工具返回原文 `{"result":"metis-t38-assert"}`（宿主模型实际调用了 MCP 工具）
4. 验收后清理：`claude mcp remove metis -s local`（避免残留测试注册）

结论：Claude Code 的 MCP 注册 + 连接 + 真实工具调用 = PASS（本机实测）。
其余 8 个 Agent 的注册断言按 AGENT_COMPAT_MATRIX 在 Phase 5 逐个处置
（可本机验证的先做；不可本机验证的 BLOCKED + 自验步骤）。
