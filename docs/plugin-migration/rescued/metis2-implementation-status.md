# METIS 2.0 — Implementation Status Ledger (持续任务账本)

> Spec section 53. States: `TODO` `DOING` `VERIFYING` `DONE` `REOPENED` `BLOCKED`.
> START_SHA: `d03921e`. Update this file after every completed item; never claim DONE without evidence.
> Feature migration map: `docs/metis2-feature-migration-map.md`.

## Progress rule
完成一项立即继续下一项，不等用户说"继续"。发现新问题自动入队。回归发现问题 `DONE → REOPENED`。禁止以"主界面已做出/测试绿/大体完成"作为停止理由（section 54）。

## Execution steps (spec section 60)

| # | Step | Status | Evidence / note |
|---|---|---|---|
| 1 | Pull latest main | DONE | on `d03921e`, tree clean at start |
| 2 | Audit old entries & dependencies | DONE | file counts + inventory in migration map |
| 3 | Build Feature Migration Map | DONE | `docs/metis2-feature-migration-map.md` |
| 4 | Build continuous task ledger | DONE | this file + 14 tracked tasks |
| 5 | Build Single Workbench | DOING | **Batch 7 (verified):** right-panel section **任务进度** `TaskProgressPanel.tsx` mounted in `ProjectsPage` chat-right column, backed by real `taskplan:*` IPC (state badges, per-state Execute/Verify/Complete/Fail/Reopen actions, generate/run-next) — real, not mocked. ProjectsPage test 5/5. Remaining: 阶段产出 + 交付成果 sections, Preview/Edit split wiring, retire topics/outcomes/submissions top-level nav |
| 6 | Migrate Project/Conversation | TODO | project-scoped conversations, left rail |
| 7 | Migrate Topic → Candidate Topic Artifacts | DOING | **Batch 25 (verified):** §7 engine primitive `engine/topic/topicConfirmation.ts` — `TopicCandidate` model, lossless `topic_<id>.md` render/parse (machine block), `buildTopicCandidateArtifact` (stage `topic`), `confirmTopic` → single `confirmed_topic.md` (errors on unknown/empty, never fabricates; non-destructive), `isConfirmedTopicArtifact`. Product-exposed via `ua:saveTopicCandidate` + `ua:confirmTopic` IPC + preload; `ArtifactsPanel`「确认为选题」on topic-stage rows + confirmed badge (works on migrated legacy topics too). 10/10 engine + 5/5 IPC tests; snapshots regenerated; typecheck+lint+arch green. Producer of candidates from live model output pending (§54) |
| 8 | Migrate Scenario → Strategy + Task Plan | DOING | **Batch 3 (verified):** `engine/research/TaskPlan.ts` RESEARCH_PLAN.md model (parse/render round-trip, 8 states, derived READY/BLOCKED, DAG+cycle validate, Execute→Verify→Save→Complete / fail→REOPEN, add/remove/move) + `taskPlanGenerator.ts` Strategy→Plan call chain. 28/28 engine/research tests. Remaining: per-project persistence, IPC, right-panel wiring, agent execution loop |
| 9 | Migrate Outcome → Stage Artifacts + Deliverables | DOING | **Batch 9 (verified):** unified contract `engine/artifacts/UnifiedArtifact.ts` (adds previously-missing `stage`+`taskId`, version chain, finality status); `UnifiedArtifactStore` (project-scoped, non-destructive versions); `ua:*` IPC + `unifiedArtifactBridge` preload; right-panel **阶段产出 + 交付成果** (`ArtifactsPanel.tsx`) mounted in ProjectsPage (version switch / promote-to-deliverable / delete). Legacy Outcomes top-level page still to be retired; Preview/Edit + old-data adapter pending |
| 10 | Remove METIS Office product integration | DONE | **Batch 37 (verified):** removed the METIS-Office editor ENTRY from the product path (`OutcomesPage.tsx`: useOutcomeGenoffice wiring + "Metis Office" actions section + dead embedded banner + guard prefixes; 2 comment brandings; unused icon import). `no-office-product-entry` PASS → **product gate 9/9**. §14.3 honored: internal Word/PPTX generation + standalone GenOffice kept. 4 office tests → 1 asserting removal; typecheck+lint+full suite 6158/0 + build + acceptance PASS. Residual dead IPC/state documented (non-gating). |
| 11 | Remove OmniRoute / free API | DONE | **Batch 16 (verified):** deleted FreeModelCenter(+css), FreeModelService, registerFreeModelIpc, freeModelBridge, and `engine/providers/discovery/{OmniRouteGateway,CommunitySourceDiscovery,ProviderDiscoveryService,NewAPIClient,AutoRegisterScheduler}`; trimmed `ModelDiscoveryStore.ts` to `MailboxPoolStore` only (mail intact); removed preload/bridge/bootstrap/appState wiring + free-API modal in ProviderProfilesSection; IPC+API snapshots regenerated; product gate `no-omniroute` PASS. General provider abstraction retained |
| 12 | Build Research Strategy | DOING | **Batches 1+5+30 (verified):** contract +`version`/`isPreset`/`StrategyPack`; 12 presets (section 9/9.1); store merges presets into `listStrategies`; `StrategyEditor` mounted in Settings 研究策略; preset copy/disable-delete; `bumpStrategyVersion` + `exportUserPack`/`importUserPack` (additive, never clobber presets). **Batch 30: 任务进度「生成研究计划」now offers a per-project Research Strategy `<select>` (over existing `strategyList` IPC) and passes the chosen `strategyId` to `taskplan:generate`, so the Research Strategy actually governs the plan built from the workbench (§12).** Settings 导入/导出 button + execution-loop wiring remain |
| 13 | Build Dynamic Capability Resolver | DOING | **Batches 8+34 (verified):** `engine/capabilities/CapabilityResolver.ts` — weighted Top-K, `required` Strategy-Core pinning, `buildSkillContext` hard zero-injection, `planMcpIds` minimal-MCP; consumed by `engine/research/TaskExecutor.ts` AND `taskExecutionService` (Execute→Verify→Save→Complete / fail→BLOCKED). **Batch 34: `planMcpIds` is now actually exercised by the real execution binding** — `executeNextTaskPersisted` computes the task's MCP set, threads it through `runModel`, records it in the execution artifact's provenance (`mcpIds`), returns it in `taskplan:execute`, and `TaskProgressPanel` shows「按本任务加载技能 N、MCP M」. Makes §26 selection live + observable (activation-by-id of MCP servers remains a follow-on). 84/84 + service tests (incl. new `loadedMcpIds` assertion). Legacy `PersonalizationResolver` all-load pending supersession |
| 14 | Build Skill Center + Skill Creation | DOING | **Batches 18+29 (verified):** Settings 技能中心 (`SettingsSkillsCenterSection.tsx`) over skill personalization IPC (list kind=skill / enable-disable via save / copy via fork / archive) → gate `skill-center-available` PASS; **Batch 29 added §23 skill-pack「导出/导入技能包」calling the already-wired `personalization:bundle:export/import` bridge (native save/open dialog + integrity handled in main).** 6/6 component tests; app typecheck+lint green; no IPC/snapshot delta (pure reuse). Remaining: 对话生成技能 (`skill:generateFromConversation` exists, model-dependent — surfaced via skillStudio elsewhere) |
| 15 | Build Prompt Engineering | DOING | **Batch 19 (verified):** section titled 提示词工程 over ArtifactPromptService (edit/reset/history/import/export); added 4 consumer-backed prompts (word.selection.rewrite/compress/expand, markdown.writing) fixing the audit's empty-defaults defect → 8 wired prompts, gate `prompt-engineering` PASS; 3/3 registry tests. §25 lists 14 slots; remaining 6 (选题生成/Task Plan/Strategy辅助/Figure Redraw/Artifact Editing/Final Review/Delivery Summary) added only as their LLM consumers exist (§25.2 no-hollow rule) |
| 16 | Build Artifact Preview/Edit | DOING | **Batches 10+31+32+33 (verified):** `ArtifactsPanel` detail = preview (text editor / image), **edit → 另存为新版本** (non-overwriting `ua:newVersion`), version chips switch current, **§18 version compare (per-version +A/−B line-diff badge via `diffLineCounts`)**, promote/deliverable, **§18 soft-delete + 回收站 (ua:archive/ua:restore/ua:purge, archived in a separate persistence category — no accidental data loss)**, **§21 导出为 Word (`exportMarkdownAsDocx` reuse)**, §7 确认为选题, §37 登记投稿. Real `ua:*` data. 14/14 panel + store trash/diff tests. Remaining: DOCX/PPTX import as deliverable, 重新生成 dispatch to Conversation, type-specific renderers |
| 17 | Build Figure Redraw + Versioning | DONE (chain) / VERIFYING (pixels) | **Batch 61:** §20 (spec-declared P0) had NO redraw path before B61 — figures in the right panel were write-once (`ArtifactsPanel` blocks 另存为新版本 for image kinds). Added the full chain on top of existing capabilities: `engine/research/figureRedraw.ts` (pure: what may be redrawn, §20's read-current-artifact context, §20.1 write plan) + `redrawFigureArtifact()` in `createDomains.ts` (§24 per-instruction capability resolution → existing `OutcomeImageService.generate` → **appended** version referencing `outcome-media:<outcomeId>/<mediaId>`) + `ua:redrawFigure` / `uaRedrawFigure` + panel affordance with §20's 7 example instructions. Tests: engine **8/8** (version chain on real SQLite: v1 preserved, v2 current, second redraw → `[1,2,3]`), IPC **4/4** (declined draw writes nothing; malformed/unauthorized never reach the backend), panel **25/25** (+6). Gate `figure-redraw-available` added → §52 **12/12** (mutation-verified). Versioning (§20.1) was already DONE via B18/B32: version chain, compare, switch-current, trash. **Residual:** real pixels need a configured image-generation provider → the live draw itself is NOT RUN; no mock is passed off as one. |
| 18 | Build Research Materials | DOING | **Batch 28 (verified):** §10 engine primitive `engine/research/researchMaterials.ts` — `ResearchMaterial` (dataset/source/upload/external/original/policy/case/other, each `ready` per §10「有则使用」) + `summarizeMaterials` (ready vs missing, `theoreticalOnly` for 纯理论) + `materialsReady` stage gate + `renderResearchMaterialsMd` grouped checklist + `buildResearchMaterialsArtifact` (stage `materials`, id `research_materials`); persisted via `ua:saveResearchMaterials` IPC (create-or-newVersion) + preload. 6/6 engine tests; electron typecheck+lint+arch green; snapshots+lifecycle count updated. Producer (upload/source-search) stays the Conversation's job, like §6/§12 |
| 19 | Build Task Execution + Validation | DOING | **Batch 24 (verified):** real execution loop wired end-to-end — `executeNextTaskForProject` binds `runModel`→`provider.complete` + `saveArtifact`→`UnifiedArtifactStore` (§24 zero-injection); `taskplan:execute` IPC + `taskPlanExecute` preload; panel「执行下一任务」runs for real (FAILED on no model, never fake). Snapshots regenerated; typecheck+lint+arch+15 engine tests green. Remaining: live run w/ a pinned free model (§54); Phase/Project validation |
| 20 | Build Reproducibility | DOING | **Batch 22 (verified):** `engine/research/reproducibility.ts` — canonical project layout + `run_all.py` generator (numeric-prefix ordering) emitted as a stage=execution `code` artifact via `ua:saveRunAll` IPC; 5/5 + 19/19 affected; snapshots regenerated; electron typecheck clean |
| 21 | Build Word/PPT Deliverables | DONE (chain) | **Batch 31 (verified):** 交付成果/阶段产出 detail gains「导出为 Word」for text-bearing deliverables, reusing the existing `outcomes:word:docx:exportMarkdown` bridge (`window.metis.exportMarkdownAsDocx({title, markdown})` → native save dialog in main) — no outcome-row coupling, works for any markdown artifact incl. §6/§7/§10/§12/execution outputs. 2 `ArtifactsPanel.test.tsx` cases (12/12); app typecheck + lint green; no IPC/method/snapshot delta. **Batch 78 (verified, LIVE):**「按模板生成 PPT」wired via new `gordenPpt:buildBriefToDisk` + `gordenPptBuildBriefToDisk` + an `ArtifactsPanel` form (template select from the real skill package, title/points defaulted from the artifact itself) — real .pptx to disk via the native dialog, real-template live build verified (21 templates, 154 edits, slide XML carries the brief's points). Gate `ppt-deliverable-available` → product gate 13/13. Remaining: DOCX→import as a deliverable |
| 22 | Build Delivery | TODO | right-panel deliverables |
| 23 | Migrate old data / compatibility | DOING | **Batches 14+15 (verified):** adapters Outcome/Topic/Scenario→unified (non-destructive, namespaced, tested) + `UnifiedArtifactStore` legacy-merge seam + **real provider bound at `createDomains.unifiedArtifactStore()`** reading `OutcomeRepository.list/get` + `TopicRepository.listSessions` (electron typecheck confirms repo signatures) → 旧成果/旧选题 now surface via `ua:list`. Pending: merge scenarios into `listStrategies`; runtime row-shape confirmation in-app |
| 24 | Real-machine full research journey | TODO | section 43 |
| 25 | Fix all P0/P1 | TODO | — |
| 26 | Two consecutive clean rounds | TODO | section 55 |
| 27 | Packaged re-verification | TODO | section 56 |
| 28 | Push | DONE | **2026-09-25: committed (`8641ebf`, campaign B1-B78) and pushed to the NEW public repo [TZUKWAN/METIS-ALL-IN-ONE](https://github.com/TZUKWAN/METIS-ALL-IN-ONE) (`main` only, ~0.2 GiB reachable history; remote name `allinone`). The original `TZUKWAN/metis-in-social-science` was not touched.** |
| 29 | Final report (START/END SHA) | DONE | section 58 — `docs/metis2-final-report.md` (B55); §10/§15/§17 refreshed at B78. START_SHA `d03921e`, END_SHA `8641ebf` |

## Product Gate (new `gate:metis2-product`, section 52)
`node scripts/metis2-product-gate.mjs` (npm `gate:metis2-product`) — live scoreboard, exit 0 only at all-PASS. Current: **13/13 PASS — product gate GREEN** (B78 added `ppt-deliverable-available`, mutation-verified). §51 note (Batch 60): this gate — plus `gate:architecture` and `gate:design-system` — is now part of **tier 1** in `build/reliability-tiers.json`, so `verify:reliability` (nightly + release triggers) enforces it; it was not before B60.
- [PASS] single workbench (任务进度 + 产出/交付 panels mounted in ProjectsPage)
- [PASS] task plan available (real `taskplan:*`)
- [PASS] strategy drives task plan (Batch 60) — `registerTaskPlanIpc.ts` must actually call `generateTaskPlanFromStrategy`, and `TaskProgressPanel` must expose `taskplan-strategy-select` + `taskplan-generate`. Closes the uncovered objective clause "Research Strategy 负责研究方法"; mutation-verified to fail alone.
- [PASS] per-task capability loading (Batch 60) — `taskExecutionService.ts` must resolve over the **current task** (`resolveCapabilities(task.title, …)`) and `createDomains.ts` must hand the model only `skillIds: loadedSkillIds`. Closes "Skill/MCP 按当前任务动态加载" against a full-injection regression; mutation-verified to fail alone.
- [PASS] artifact panel available (real `ua:*` 阶段产出)
- [PASS] deliverable panel available (final-status 交付成果)
- [PASS] no scenario primary nav — **Batch 36: topics/outcomes/submissions demoted from `getPrimaryResearchNav` (return []); their primary workflows absorbed into the workbench, standalone pages kept reachable via command palette + Global Search as a reversible escape hatch (no capability deleted). App/nav tests updated, both python harnesses trimmed to projects+scenes; build + acceptance:layout PASS, full suite 6161/0.**
- [PASS] no office product entry — **Batch 37: removed the METIS-Office external-editor entry from `OutcomesPage.tsx`** (the "Metis Office" launch button + sync/discard controls + the dead `Metis Office 原生编辑` embedded banner + `useOutcomeGenoffice` wiring; stripped 2 comment brandings). §14.3 honored: internal Word/PPTX generation + the standalone GenOffice app are untouched; the product no longer embeds an office editor. The 4 office-flow tests replaced by one asserting the entry is gone. entryGone + brandingGone both true.
- [x] no OmniRoute — FreeModelCenter + discovery services deleted (Batch 16); `ModelDiscoveryStore.ts` trimmed to `MailboxPoolStore` (submission mail retained)
- [x] skill center available — Settings 技能中心 (`SettingsSkillsCenterSection`, real skill IPC CRUD)
- [x] prompt engineering available — Settings 提示词工程 (`SettingsOutcomePromptsSection` over ArtifactPromptService, 8 consumer-backed prompts, edit/reset/history/import/export)

## Final stop conditions (section 57) — all must be PASS/DONE

Legend: **DONE** = implemented + verified by automated tests/gates; **PARTIAL** = code present but the real-machine / model-gated journey not yet verified; **TODO** = not done.

| Gate | Status |
|---|---|
| Feature Migration Map = DONE | DONE (v1；A 节六行状态已于 B68 按实际完成情况刷新，不再只有"TODO") |
| Scenario user entry = 0 | **DONE (B40, machine-verified)** — the 场景 tab is removed from `PersonalizationCenter` (lands on the MCP extension library), `ScenarioWorkbench` is unreachable from any user path (only the internal `initialKind` test prop), the nav entry is relabeled 场景→扩展 / Scenarios→Extensions, and the write-mode `ScenarioLauncher` "打开场景" CTA is gone (plan inspector → `TaskProgressPanel`). Acceptance harness asserts `.scenario-workbench === 0` at the personalization nav across 6 viewports → `acceptance:layout` status **passed**. Scenario capability lives on as Research Strategy (`StrategyEditor` in Settings; legacy scenarios auto-project via `scenarioToStrategy`, §38). |
| METIS Office product entry = 0 | DONE (B37; `no-office-product-entry` PASS) |
| OmniRoute = 0 | DONE (B16; `no-omniroute` PASS) |
| Single Workbench = PASS | DONE (`single-workbench` PASS; 3-pane + nav demoted) |
| Project/Conversation = PASS | DONE (project-scoped chat + §32/§18 chatIntent bridges) |
| Literature Stage = PASS | **LIVE for the stage artifact (B66); model-gated part is retrieval quality only.** §6's `literature_library.md` used to be producible only by an IPC call no screen made: 右栏「沉淀文献库」now reads the real paper library (`listPapers`) and files this project's entries under **① 文献准备** with a GB/T 7714 / APA choice, asserted live (`label ① 文献准备`, `已把本项目的 1 篇文献…`) and surviving a restart. That step is also what exposed defect #15 (`paper:save` silently discarding the `projectId` its own contract advertises) — and #15's sibling, defect #22 (the same writer discarding `starred / archived / priority / deadline / readingProgress / readingTimeSeconds / readAt`), is FIXED in B74: migration 119 adds the columns, `savePaper` carries them forward, `paper:list` stops hiding them, and a ★ now survives a hard quit (relaunch assertion + the 资料 tab itself, where un-starring takes effect). What still needs a model: whether the Conversation finds good literature in the first place. |
| Topic Artifacts = PASS | **DONE and now user-reachable end to end (B66).** 右栏「录入候选选题」writes a `topic:<id>` stage artifact (② 选题确认), and the pre-existing「确认为选题」on that row turns it into the project-wide `confirmed_topic.md` — both halves asserted live (`row true / label ② 选题确认 / confirmed true / hadConfirmedBefore false`) and durable across restart. Exploring topics conversationally (F2) remains a separate open line. |
| Research Strategy = PASS | DONE (presets + editor + §12 workbench picker). **B47:** §9 "strategy must really shape the Task Plan" now deterministically proven — `tests/engine/taskPlanGenerator.test.ts` asserts one dependency-chained task per phase (title=phase.name, section from phase.action, acceptance from phase.prompt w/ fallback, first=READY/downstream=TODO) and that two strategies yield materially different plans.  Resolves the old "strategy→Task-Plan influence pending" migration-map caveat. **B70 corrects an important limit of that proof:** B47 called the generator directly, so it never exercised *resolution*. `getStrategy()` read only persisted rows while the picker lists presets + legacy projections — choosing any built-in silently generated with the default strategy (defect #19, P1). Now every listed id resolves (invariant pinned in `tests/engine/StrategyResolution.test.ts`, 5/5), the panel keeps its strategy picker after a plan exists so re-planning is reachable (#20), and re-planning no longer merges outputs of different strategies into one artifact (#18, `taskArtifactId`). Live: sweep step 4i picks `preset_theoretical_interpretation`, regenerates, executes, and sees a **new** `task:T001:问题化` row under ② 选题确认 while the previous `task:T001:文献综述` keeps its stage and its 2 versions untouched. |
| Task Plan = PASS | DONE (model + real execution wired) |
| Dynamic Skills = PASS | DONE selection (B48) **and** per-task MCP lifecycle (B63) — **B48:** §24/§26 guarantees now directly tested (`tests/engine/CapabilityResolver.test.ts`, 4/4): zero-injection (unselected skill body ABSENT from `buildSkillContext`), `required` Strategy Core always loaded, `planMcpIds` = only selected skills' MCP servers, title>description relevance weighting. Activation-by-id itself is implemented (B63): `MCPManager.startForTask/releaseFromTask` around the shared `AgentLoop` with a per-task `allowedTools`, released in `finally`, verified against the real stdio fixture — the open part is only a model-initiated tool call on a user-configured server (needs credentials) and per-server boot autostart (a behaviour change). **Finding corrected in B61's audit:** §26 asks for resolve → start → expose → execute → **release** per task, and the last three are NOT implemented — `MCPManager.loadAndConnectAll()` (`engine/mcp/MCPManager.ts:42-47`) connects every *enabled* server eagerly at boot and `getAllTools()` hands out the global set, so there is no on-demand connect/disconnect around execution; "默认不启动所有 MCP" therefore holds only in the weak sense that disabled servers stay off. The consent path (definition enablement) is a separate, already-tested concern and must not be bypassed. Cheapest correct slice + mandatory re-validation recorded in `docs/metis2-final-report.md` §6 (per-server boot-autostart default off; connect the not-yet-connected enabled servers in `planMcpIds`, expose their tools to that execution, disconnect in `finally`; then re-run `vitest-mcp-leak`, `child-process-leak-gate --cycles=20` and two live sweeps). **§26's other half was implemented in B63** (the audit above was right that it was missing, and wrong to leave it there): `MCPManager.startForTask/releaseFromTask/toolsForServers/toolNamesForServers` + task execution routed through the existing `AgentLoop` with a per-task `allowedTools` allowlist, released in `finally`. A task can never start a disabled or unknown server (reported as `unavailable`) and never cuts a connection it merely borrowed. Verified against the real stdio MCP fixture (`tests/electron/McpPerTaskLifecycle.test.ts` 3/3), `ChildProcessLeakGate`, `child-process-leak-gate --cycles=20` PASSED, and two clean live sweeps afterwards. Still open on this line: a model-initiated MCP tool call on a real configured server (NOT RUN — needs user credentials/config) and per-server boot autostart (behaviour change, deliberately not bundled). |
| Skill Center = PASS | DONE (`skill-center-available` + §23 pack import/export) |
| Skill Creation = PASS | **DONE for the product surface (B54); the distillation call itself stays provider-gated.** The old note ("not surfaced as a workbench action") was **wrong on inspection**: `/skill` is a first-class entry in `SLASH_COMMANDS` (`src/lib/slashCommands.ts:27`, with arg placeholder), so it is offered by the composer autocomplete and listed by `/help`, and `src/conversation/chatTurnFlow.ts:605-634` implements the whole path. A learn-skill **button** deliberately does not exist — `ChatPageStreaming.test.tsx` asserts it was "removed per 2026-09 spec" — so adding one would have reverted a product decision; the unused `chat.learnSkill` i18n key is the leftover label of that removal, not a missing feature. What was actually missing was verification: **nothing tested that a conversation reaches the extractor.** Added 2 tests (composer `/skill <意图>` → preload bridge → installed-skill report, incl. the too-short refusal that must not call the model) → `ChatPageStreaming` **12/12**. Residual: the real distillation quality/behaviour with a live model is unverified here because the mocks prove wiring, not model output. |
| Prompt Engineering = PASS | DONE (`prompt-engineering-available`) |
| Research Materials = PASS | **LIVE (B66).** §10's inventory was another handler-with-no-call-site: 右栏「补录研究材料」now records 名称/类别/位置/格式/已就绪 rows through `uaSaveResearchMaterials`, files them under **④ 研究材料**, refuses an empty inventory instead of writing a hollow artifact, and the artifact (with its marker) is asserted to survive a restart. §10's "有则使用，没有则调用数据获取" acquisition side still runs through the Conversation/tools, not this form. |
| Task Execution = PASS | **B58 verified end to end on real Electron; B59 additionally asserts WHERE the output lands.** `executeNextTaskPersisted` has deterministic engine tests (B46, 4/4), and the full user path is measured live in `ui-feature-sweep`: real UI creates a project → right panel mounts → strategy picker → generate produces 4 task rows → **run-next executes** → badge `完成` with the next task flipping to `可执行` (§30 derivation live) → **the output appears as a 阶段产出 row** (`rowsBefore 0 → rows 1`) **and is filed inside `stage-group-execution` / 「⑤ 分析执行」** — `taskplan:execute` stamps `stage:'execution'` (`createDomains.ts:316`), so this is §5's lifecycle presentation, not just storage; a throwaway mutated copy of the harness scored 40/41 to prove that assertion can fail. That run exposed and fixed a real defect: `ArtifactsPanel` only reloaded on `projectId` change, so an output saved into the active project stayed invisible — see Batch 58. Remaining honest limit: the executing runtime here is the acceptance harness's loopback provider (the app's own provider stack, real IPC, real SQLite); a commercial provider's answer quality is still `NOT RUN` for want of credentials. **Updated by B67/B69/B70/B72:** the fixed 「⑤ 分析执行」expectation is gone — output now files under the §5 stage its own strategy phase maps to (`engine/research/strategyStageMap.ts`), the harness asserts that from its own independent table, multiple executed tasks accumulate in multiple stages (60/60 assertions as of B74), artifacts are keyed `taskArtifactId` so a regenerated plan cannot merge phases into one row, and step 4i drains a 13-phase plan so ①②③⑤⑥ are each pointed at live. |
| Reproducibility = PASS | **DONE in code, NOT wired to any user action — and left that way on purpose (B66 audit).** §12's engine + `ua:saveRunAll` + tests exist, but the input is an ordered list of script paths (`ReproCodeStep[]`) and nothing in the product produces such a list; a form would have to invent the steps, which is the §25 no-hollow rule inverted. `uaCreate` (a generic "file this as a stage output") is likewise uncalled — the honest place for it is the Conversation's preview pane, recorded as an open item rather than closed with a fake button. |
| Stage Artifacts = PASS | **All seven §5 spine stages now have a producer, and five of them are asserted live.** B66 gave the panel its own forms (① 文献准备 from the real paper library, ② 选题确认 candidate → 确认为选题, ④ 研究材料); B67 replaced the hardcoded `stage:'execution'` stamp with the strategy-action table `engine/research/strategyStageMap.ts`, so a task's output files under the stage its own phase belongs to — ①②③④⑤⑥ are all reachable from a generated plan (live: a `相关研究` task's row landed in **① 文献准备**, harness expects from an independently hardcoded table); B68 files the exported submission package as **⑦ 交付** (real package id / dir / file list / preflight verdict, version-appended on re-export, and the first product call site for `ua:create`). Live-verified: **all seven stages have been pointed at on real Electron** — ①②④⑤⑦ by B66–B68, and ③⑥ by B72, whose step 4i now drains a re-planned 13-phase strategy (`executions 13`, `stagesCovered = [design, execution, literature, outcome, topic]`, every row's DOM group agreeing with the store's `stage`, `untouched: true`). Remaining honest limit: `uaSaveRunAll` (⑤'s reproducible-run document) still has no user entry because nothing in the product produces ordered script paths — recorded, not faked. |
| Artifact Preview/Edit = PASS | DONE (§18/§19 edit→version + export + trash + compare) |
| Figure Redraw = PASS | **NOT RUN (chain DONE, pixels blocked)** — B61 built and deterministically verified §20 end to end (engine 8/8 on real SQLite, IPC 4/4, panel 25/25, gate `figure-redraw-available`), but this stop condition means an actual figure was redrawn on screen. That needs a configured image-generation provider (设置 → 图片生成), which this machine does not have; no mock/static image is substituted. |
| Artifact Versioning = PASS | DONE (version chain + §20.1 compare/restore) |
| Word = PASS | PARTIAL — §21 export DONE; in-app rich edit intentionally moved to standalone GenOffice (§14) |
| PPT = PASS | **DONE + LIVE (B78)** — the last unwired §21 row: a right-panel 交付成果 can now be turned into a real .pptx. New `gordenPpt:buildBriefToDisk` (same GordenPPTSkill build chain as the retired Office ribbon: ensureSkillPackage → buildFromBrief → PptDocumentSchema re-validation → native save dialog, with the DOCX exports' `METIS_E2E_EXPORT_DIR` seam), preload `gordenPptBuildBriefToDisk`, and an `ArtifactsPanel` form (「按模板生成 PPT」) whose 标题 defaults to the artifact title and 要点 defaults to the artifact's own markdown list lines (`briefPointsFromMarkdown` — extraction only, no invention). **LIVE on this machine:** real `git clone` of GordenPPTSkill (21 templates enumerated), real python-pptx build (`selectedSlides [1,2,3]`, **154 slot edits**), real `OutcomePptxService.exportFile` bytes (11,748 B, PK zip magic), and the exported deck's `ppt/slides/slideN.xml` **contains the brief's own points** (「平台进入如何改变村庄信息结构」「信任机制…」「县域案例」); deck kept at `logs/gorden-live-deck.pptx` (gitignored). Template-list / build failures surface verbatim in the panel; no mock artifact anywhere. Gate `ppt-deliverable-available` added → §52 **13/13** (mutation-verified: renaming the channel flips it FAIL). |
| Delivery = PASS | **B43 advanced**: §17/§29 delivery-package actions surfaced in the right panel — 投稿案件「预检」(`submission:preflight:run` → blocker/warn list) +「生成并导出材料包」(`submission:package:assemble`→`export` → dir + counts) on each tracked case, reusing existing IPC (no new channel/snapshot). promote-to-final + register/track already present. **-now LIVE (B65):** the whole chain is driven by real clicks in `ui-feature-sweep` on real Electron — 登记投稿 → 预检 (`预检通过（13 提示）`) → 生成并导出材料包 (`材料包已导出 1 项：<profile>/submissions/<caseId>/round-1`, verified from the main process with `fs.existsSync` + non-empty directory) → 校验 (`1 有效 · 0 无效 · 0 待确认 · 0 待定`) → 冻结 (`材料包已冻结` + 已冻结 badge), with the freeze assertion written conditionally against the preflight verdict so it cannot pass vacuously. That step also exposed a real P1 (defect #14: `registerSubmission` never refreshed `cases`, so the panel claimed a case while 投稿进度 still said 尚无投稿案件) — fixed with `await reload()` plus a jsdom regression, 26/26. Still open on this line: §37 选刊 shortlist / submission-assistant chat / portal browser remain (model + external-site features). |
| Old Data Compatibility = PASS | DONE (§38 outcomes/topics/scenarios surface non-destructively) — **with one caveat now closed**: the Scenario→Strategy projection was producing contract-invalid rows until B53, which made `strategy:list` come back empty for any user with a legacy scenario (i.e. §38's promise was silently inverted into a loss). Fixed + regression-tested in `tests/engine/LegacyScenarioStrategyRoundTrip.test.ts`. |
| P0 = 0 / P1 = 0 | **PARTIAL (B49→B66)** — a formal silent-data-loss sweep is now running (task #18) and has already found + fixed **5 real P1 data-loss defects** (B49 ×4 + B53: `strategy:list` empty for any user with a legacy scenario, because §38's `scenarioToStrategy` emitted ids violating `RuntimeIdSchema`), each reproduced before being fixed: `metadata.stepCard` rejected → scenario messages disappear on reopen; `createArtifacts` FK rollback → generated artifacts destroyed on the mainline `'global'` chat path; `deleteSession` unable to delete a `'global'` conversation holding artifacts; `paper:list` returning `[]` for any non-empty library. Not yet claimable as 0/0: the triage register is now written up — `docs/metis2-final-report.md` §18.1 lists **16 P1 rows registered, 15 fixed with regression coverage** (the register's own status column is the authority; re-counted after B75) (stepCard history, artifact FK rollback + `deleteSession` under the `'global'` sentinel, save-false-report mirror, `paper:list` emptied, `strategy:list` emptied, paused-Goal enum downgrade, right-panel staleness, 投稿登记后不刷新, **`paper:save` 丢弃自己契约里声明的 `projectId` (B66)**), **1 spec-declared P0 feature gap closed** (§20 figure redraw, B61), **1 P2 caught before ship** (drawing-prompt budget), (#18 task-output identity by slot → `taskArtifactId`, #19 `getStrategy` could not resolve the presets the picker lists → 选策略被静默忽略, #21 打包验收在半途抛错时仍判 PASS → 强制检查清单 + 有界重试, #22 文献条目七项阅读元数据写入即丢 → 迁移 119 补列 + 视图不再 omit + 真机跨重启与「资料」页签各一条断言（B74）, #24 笔记 ★ 是同一形状的第二张表 → 迁移 120 + 真实 IPC 往返（B75），并附 #25 那一行把同一方法机械套到 collections / experiments / mcp_servers / workflow+eval runs 后的排查结论), plus two capability-class entries from B66: **#16** four stage-artifact IPCs with no user call site (three wired + live-asserted, `uaSaveRunAll`/`uaCreate` recorded OPEN because nothing in the app produces their input — inventing a form would fabricate content), and **#17** the orchestrator's stale numeric tripwire that made every report cry "数字偏差" unconditionally. The session/experiment decoders were verified NOT live and pinned. Two of B49's "latent, not live" guesses were wrong in the informative direction: `pauseRequested` was live and became defect #6. Formal sign-off is still the user's call, so this row stays **未签署**. |
| Full Sweep #1 / #2 = PASS | **PASS (B56 → 成对复跑至 B75) — 当前树连续两轮：A 15:28:23Z→15:31:15Z / B 15:31:17Z→15:34:08Z，各 7/7 步骤 exit 0，扫掠 61/61、重启子阶段 12/12，`noteSeed` / `literatureSeed` / `replanningIsolation` 等被比对的四份证据块与整份 `durability` 两轮逐字节一致。** `phase2-full-sweep --label=A` and `--label=B`: each **7/7 steps, exit 0**, identical numbers (`ui-feature-sweep` **41/41** · `ui-resolution-matrix` 106/106 across 6 viewports · `channel-probe` · `acceptance:layout` · `gate:architecture` · `gate:design-system` · `ui-baseline-capture` 49 shots / 0 warnings); manifests in `logs/phase2-sweep-{A,B}/` (B59 pair finished 07:46:16Z / 07:48:40Z, 144 s each). **Two earlier claims were wrong and are corrected here:** (a) "needs seeded profiles + a live model" — the orchestrator drives its own loopback provider, so no model is required; (b) the sweeps could not pass on the converged app at all because the three `ui-*.cjs` harnesses still clicked the retired `topics`/`outcomes`/`submissions` destinations and waited for the §33-removed `scenario-workbench` (round A scored 4/7 for this reason). The harnesses' stale IA assumptions were fixed — not the product. Disclosed in `docs/metis2-final-report.md` §16: matrix assertions fell 196→106 because three surfaces described pages that no longer exist. The right-column project-state mount that B56 recorded as `NOT RUN` was later asserted for real (B57), and so is the executed task's placement on the §5 stage spine (B59). |
| §58 最终交付报告 = 存在 | **DONE (B55)** — `docs/metis2-final-report.md`: all 20 mandated sections, `START_SHA`/`END_SHA` (`d03921e`, identical — see the uncommitted-tree note), and every verdict restricted to `PASS` / `FAIL` / `NOT RUN` / `BLOCKED` with an evidence pointer. It records 10 open items rather than rounding them off, including the §57 items still `BLOCKED` (commit → tier-4 release chain → NSIS smoke; §26 MCP consent activation) and `NOT RUN` (live-model journeys, CI, two-round feature sweep on the current tree, formal P0/P1 triage sign-off). |
| Packaged App = PASS | **see the two Packaged rows immediately below — the packaged runtime is now VERIFIED (35/35 CDP assertions, B51/B52); this row is kept only for the ABI history.** Original **PARTIAL (B45)** — `npm run pack` (`electron-builder --dir`) **exit 0 at current head** → `release/win-unpacked/Metis Research Workbench.exe` produced, genoffice runtime staged (34 pkgs), binaries signed ⇒ the §56 packaging pipeline works on the converged app. Not yet a PASS: pack log shows `npmRebuild is set to false` so it baked the CURRENT (node-ABI) `better_sqlite3` → the win-unpacked app can't open SQLite under Electron; a runnable+verified packaged build needs pack under Electron ABI (`rebuild:electron` first), AND `scripts/agentc-pack-final-verify.cjs` still has B36 nav staleness (`clickNav('topics'|'submissions')` on demoted destinations, §33 `.scenario-workbench` section already fixed in B41) → refresh + a full CDP smoke (§56) to flip to PASS. |
| Packaged row — **correction (B50)** | **The migration suggested in B45 is void.** `scripts/agentc-pack-final-verify.cjs` is an **orphan**: it is referenced by neither `build/reliability-tiers.json` nor any `package.json` script, so its staleness (hard-coded 581 bridge count, `clickNav('topics'|'submissions')` — both destinations confirmed removed from `src/shell/navConfig.ts`, whose primary nav is now only `projects`/`settings`/`personalization`) cannot affect any gate. Do not spend a batch retargeting it. §56's authoritative gates are the **tier-4 manifest entries**: `release-chain` = `npm run release:windows` (provenance → build:electron → electron-builder `--win nsis msi` → SBOM → scan → package → verify; purely local, no publish) and `install-upgrade-smoke` = `node scripts/install-upgrade-smoke.cjs`. `release:windows` also inherently fixes the `npmRebuild:false` ABI defect, because `build:electron` runs `electron-rebuild` immediately before electron-builder packs the current tree. Tracked as task #19. |
| Packaged row — **B51/B52: packaged app VERIFIED RUNNING (35/35, exit 0)** | `rebuild:electron` → `npm run pack` (both exit 0) → `release/win-unpacked/Metis Research Workbench.exe` (223 MB, Electron-ABI sqlite) driven over real CDP: **`--- pack-final verify: PASS (35/35) captures=6 ---`, `SMOKE_EXIT=0`**, covering startup-ready (⇒ SQLite opens: the B45 ABI defect is gone), one real streamed chat with full 40-chunk reassembly via the loopback provider, §15 retirement invariants, extensions/skill-MCP center with `.scenario-workbench === 0`, settings dialog portal + viewport centering, accent switched to gold by real swatch click, AIO zen mode in/out, and **close + relaunch on the same profile**. The path there is the useful part: the first run scored 31/36, and each failure was diagnosed rather than excused — 1 stale `=== 581` (measured 589 = current snapshot), 3 asserting **retired** destinations (`getPrimaryResearchNav()` returns only `personalization`, so the product was right and the harness wrong), 1 intermittent packaged relaunch failure. Harness fixes in `scripts/agentc-pack-final-verify.cjs`: bridge surface now compared to `tests/electron/fixtures/metis-api-snapshot.json` as a **set** (drift in either direction becomes visible); retired-destination sections replaced with "absent" invariants plus an honest empty-state contract for the project-scoped right panels (its project-state mount is recorded in `report.packagedRightColumnGap` as unit-layer coverage, **not** faked); child stdio captured per launch (was `stdio:'ignore'`, which made the relaunch failure undiagnosable); and the relaunch race root-caused — `waitPortAndPage` captures `DevToolsActivePort` **once** and then polls that port forever, so a file left by session 1 pinned it to a dead port → stale port file is now deleted before each launch, which is what took the run to 35/35. Evidence that the app itself was never at fault: the relaunched child log shows `[Main] active provider profile restored`, i.e. persisted state survived close+relaunch in packaged form. **Re-proven at B61 / B63 / B65 / B68 and again after B70+B71 (two consecutive 35/35 runs, 590 bridge methods, 13:12:06Z + 13:12:29Z). **B71 also fixed the verifier itself**: `pass` used to be `checks.every(ok)`, so a run that threw during the relaunch stage and never wrote those 4 checks still printed `PASS (31/31)` and greened tier-4; it now requires zero failures + no recorded error + all 11 mandatory check names present, and the relaunch race itself is handled by a bounded retry on transient CDP `Execution context was destroyed` (assertions unchanged) (`npm run pack` exit 0 → `pack-final verify: PASS (35/35) captures=6`, `logs/b61-pack.log` / `logs/b61-packverify.log`), so this row is not an inherited B52 conclusion; the bridge-set check also confirms the new `uaRedrawFigure` / `ua:redrawFigure` surface is present in the packaged exe. **Still-blocked requirement:** `npm run release:windows` (tier-4 `release-chain`) fails closed at its FIRST step — `release-provenance-lib.mjs::assertReleaseGitState`: "commit or otherwise clear all tracked and untracked source changes before producing a candidate." ⇒ tier-4 `release-chain` + `install-upgrade-smoke` (hence NSIS install smoke) **cannot run until this session's work is committed**, and §57's "Working Tree = CLEAN" row is the same gate. No bypass attempted (no stashing the tree away, no flag overrides) — it is a release-integrity gate, so this needs your commit decision. |
| CI = PASS | **PASS (2026-09-25) — first real GitHub Actions run on the new public repo: [Quality gates run 36050357986](https://github.com/TZUKWAN/METIS-ALL-IN-ONE/actions/runs/36050357986), all 8 jobs green (type-check+lint · convergence gates · 4 test groups incl. frontend 6m22s · electron build · advisory security). Previously NOT RUN (needed a push); the pipeline has been aligned with §51 since B60 and every command in it was also executed locally (B60).** Tier 1 of `build/reliability-tiers.json` (what `nightly.yml`/`release.yml` enforce via `verify:reliability`) now carries the three convergence gates (13 → 16 checks, all measured PASS through the authoritative runner); `ci.yml` gained a `convergence-gates` job and lost a matrix path pointing at the never-existing `tests/docs`. `tests/scripts/OpenSourceMetadata.test.ts` pinned that phantom path, so it was repointed to assert the gates instead — plus a new test that the manifest really holds them at tier 1 (4/4 green). No GitHub Actions run is observed, so this row is NOT PASS. |
| Working Tree = CLEAN | **DONE (2026-09-25)** — the whole campaign committed as `8641ebf` (on top of `d03921e`); local pack garbage-collected (unreachable orphans 3.29 GiB → dropped; main's reachable history ~0.2 GiB, no blob >20 MB); pushed to METIS-ALL-IN-ONE |

## Engineering gates to keep green (section 51)
`typecheck` · `lint` · `ipc:snapshot:check` · `gate:architecture` · `gate:design-system` · `vitest` · `verify:reliability` · ui sweep · resolution matrix · packaged smoke.

**The ABI constraint that decides tier membership (learned the hard way, B73):** `better-sqlite3` is a single-ABI native module, so a tier that mixes an in-process vitest check (needs the Node ABI) with gates that spawn the built Electron app (need the Electron ABI) can never pass as a unit — that is why `vitest-mcp-leak` moved into tier 1 at B73 and tier 2 is now exactly the four app-spawning gates. Re-verification order on one tree: `build:electron` → tier-2 + §55 sweeps → `rebuild:node` → tier-1 + full `vitest` → `build:electron` again → pack + tier-4.

> **Authoritative §51 gate = the tiered `verify:reliability` manifest (`build/reliability-tiers.json`), NOT the loose `scripts/ui-*.cjs` probes.** Re-run after §33 (2026-09-23): **Tier 1 (PR-required) = PASS 13/13, 0 failed, 0 not-run** — typecheck · lint (against `build/lint-baseline.json`) · audit · better-sqlite3 host-ABI · vitest contract-schema · security · IPC-snapshot · update-trust · provider-failure-matrix · context-isolation · schema-gate · backup-system · health-diagnostics. This supersedes the earlier worry (B41) that `ui-feature-sweep.cjs`/`ui-resolution-matrix.cjs` were gates — they are developer CDP probe scripts, not in the manifest, so their stale B36 nav refs are NOT a §51 blocker (only relevant if run manually). Tier 2 (desktop acceptance: `acceptance:layout` + shutdown/relaunch + child-process-leak + crash-relaunch + mcp-leak) and Tier 4 (`release:windows` package/install) are real-Electron/packaging gates — runnable here (proven) but each is a heavy multi-minute CDP/packaging run, executed in the §43–58 acceptance phase.

> **Tier-2 desktop acceptance first run (2026-09-23):** `acceptance-layout` **PASS**, `vitest-mcp-leak` **PASS**, `child-process-leak-gate` (20 cycles terminal+genoffice) **PASS**. But `acceptance-shutdown-relaunch` **FAIL** and `crash-relaunch-harness` **FAIL** — and every crash-relaunch assertion returned `None` (relaunched-app-usable / crash-marker / DB-integrity / outcome-read / payload-complete all `None`; `agent-run-mid-stream` 0 assertions; `migration-failure-fixture` `None`). All-None = the harness never attached to the relaunched app (no measurements) → consistent with **environmental** causes, NOT a §33 regression: crash/shutdown/relaunch test lifecycle+migration+persistence code I never touched, AND the run mixes electron-app gates with node-vitest gates over a single shared `better_sqlite3.node` ABI (tier-2 needs an electron-ABI build for the spawned app but node-ABI for the vitest sub-gates; running them in one pass against a mismatched bundle yields un-bootable relaunched instances). Plus ~25 stray node.exe from prior builds (Electron is single-instance → contention blocks relaunch). **ACTION for §55/§56:** re-run tier-2 in a clean process env (`build:electron` so the app bundle is electron-ABI, kill stray electron, run the electron-app gates, then `rebuild:node` for the vitest gates) to classify environmental-vs-real before treating any crash-relaunch/shutdown failure as a defect. Do NOT record line 88/89 as PASS on this run — 2 required desktop gates are red/unmeasured.

> **Tier-2 clean re-run (2026-09-23, Electron-ABI bundle):** ran the two failed gates in isolation after `build:electron`. **`crash-relaunch-harness` → PASS** (all 3 scenarios) → confirms the earlier all-None was purely the Node-ABI/sqlite mismatch for the spawned app, NOT a defect and NOT §33-related. **`acceptance-shutdown-relaunch` → still FAIL but now real: 10/11 sub-assertions pass**, single failure = "approved result is persisted through the real Electron SQLite path" — history reads back the user message + 2× `{kind:recovery, code:history_item_unavailable}` instead of the assistant's approved-result rows (producer `engine/runtime/ChatRuntimeContract.ts:344` `if (!row) return historyRecovery('history_item_unavailable')`). The E2E drives scenario RUNTIME execution (`user:scenarios/shutdown-relaunch-approval-e2e`, controlled loopback provider) — it does NOT use the §33-retired personalization UI/ScenarioLauncher, and no convergence batch touched the chat-history/approval-result persistence path. So this is a genuine PRE-EXISTING desktop-acceptance defect (the gate hadn't run since B36) in approved-scenario-result history persistence, OUTSIDE the §1-§60 convergence scope. **Action:** needs a focused chat-persistence investigation (approved-scenario assistant turns not landing as history rows, or a relaunch read-before-write race) — track as a §55 desktop blocker, do not conflate with §33. §51 tier-1 (13/13) and §33 remain solidly green; Tier-2 is 4/5 PASS with 1 real pre-existing failure.

> **Tier-2 blocker RESOLVED (Batch 49, 2026-09-23):** `acceptance-shutdown-relaunch` now **PASSES** on real Electron (`node scripts/electron-shutdown-relaunch-approval-e2e.cjs` → `E2E_EXIT=0`, 9/9 outer assertions, phase-1 + phase-2 `passed`). The single remaining 10/11 failure was NOT one bug but three, each reproduced before being fixed: (a) `HistoryRunMetadataSchema` strictness rejected `metadata.stepCard`, so scenario assistant messages decoded as `history_item_unavailable` and disappeared; (b) `createArtifacts` mirrored into `research_artifacts` using the raw `sessions.project_id`, and the `'global'` sentinel the chat shell writes has no `projects` row → `FOREIGN KEY constraint failed` rolled the artifact itself back ("生成物注册失败，请检查存储"), with `deleteSession` equally unable to remove such a conversation; (c) `mirrorScenarioArtifactToResearch` threw *inside the same try* as the save, so a committed artifact was still reported as a storage failure. Full detail in Batch 49 below. The e2e assertion itself was also stale (it demanded the marker in the chat bubble although production persists full output as a 生成物 plus a summary) — retargeted to read the marker back from the persisted artifact over real IPC, one assertion replaced by two stronger ones, none removed. **⇒ No known Tier-2 desktop-acceptance blocker remains.**

> **§51 re-measured (2026-09-24, B60→B61, current tree):** **Tier-1 = 16/16 PASS** — `node scripts/reliability-gate.mjs --tier=1` → `gate: PASS (passed=16 failed=0 advisoryFailed=0 notRunRequired=0 notRunOptional=0)` (`logs/reliability-gate-report.json`, `logs/b61-tier1-full.log`). The list grew from 13 to 16 because B60 finally put the campaign's own convergence gates (`gate-metis2-product`, `gate-architecture`, `gate-design-system`) into the PR-required tier — before that, nightly/release enforced a tier that could not see a re-introduced retired surface. **Full `vitest run` = 6215 passed / 0 failed / 9 skipped (6224), 581 files, exit 0** (`logs/b61-vitest-full.log`; arithmetic: 6193 at B58 + 1 at B60 + 21 at B61, nothing deleted or skipped for green). typecheck(4) 0, `eslint .` 0 errors, `gate:metis2-product` **12/12**, arch 602/0 cycles, design PASS, `ipc:snapshot:check` no drift, pytest 12/12. §55 two consecutive clean rounds re-run after B61's source changes (A 08:46:19Z / B 08:48:46Z, 7/7 steps each, sweep 41/41, matrix 106/106, capture 49/0), and the packaged exe was re-packed and re-verified 35/35 on this same tree.
>
> **§51 re-measured again (2026-09-24, B62→B63):** B62 registered the packaged CDP journey as **`packaged-cdp-smoke` in tier 4** and fixed its stale default packed dir; B63 implemented §26's `start → expose → release` per task. On that tree: typecheck(4) 0, `gate:metis2-product` **12/12**, arch/design/ipc PASS with no drift, **§55 rounds A 09:29:17Z + B 09:31:51Z each 7/7 exit 0** (sweep 41/41, matrix 106/106, captures 49/0), `child-process-leak-gate --cycles=20` **PASSED with all ports free**, `npm run pack` → **tier-4 `packaged-cdp-smoke` PASS (35 checks, 590 bridge methods)**, `rebuild:node` → **full suite 6218 passed / 0 failed / 9 skipped (6227), 582 files** (`logs/b63-vitest-full.log`), **tier-1 `gate: PASS (passed=16 failed=0)`** (`logs/b63-tier1.log`), pytest **12/12**.

> **§51 re-measured (2026-09-25, B77→B78, current tree — the delivery ladder):** typecheck(4) 0 · lint-gate clean · full `vitest run` **6285 passed / 0 failed / 9 skipped (6294), 589 files** — after hardening the jsdom project to `testTimeout: 30_000` (same rationale as the node project's existing 30s): three consecutive full-suite runs on 2026-09-25 produced 2-12 *rotating* frontend render timeouts at the 5s default while each file passed in isolation; with the config fix the suite is deterministic-green (and 783 s vs 1270-1354 s, because the flaking files no longer burn their retries). pytest **12/12**. `build:electron` PASS → **§55 rounds A + B both 7/7 steps, exit 0** on this tree (`logs/phase2-sweep-{A,B}/`) → `npm run pack` exit 0 (223 MB exe, Electron-ABI sqlite) → **tier-4 packaged CDP smoke `PASS (35/35) captures=6`, SMOKE_EXIT=0** (incl. close + same-profile relaunch persistence) → `rebuild:node` → **tier-1 `gate: PASS (passed=18 failed=0)`** (`logs/reliability-gate-report.json`). Product gate **13/13** (B78's `ppt-deliverable-available`, mutation-verified).

Verified at head (2026-09-23): typecheck (all 4 tsconfigs) · lint · `ipc:snapshot:check` · `gate:architecture` · `gate:design-system` · `gate:metis2-product` (7/9 scoreboard) · full `vitest` (6000+) · **`vite build` renderer production build PASS** · **`build:electron` FULL PASS** (genoffice staging + `electron-rebuild` better-sqlite3) · **`acceptance:layout` Electron boots + CDP layout harness PASS** (per-viewport screenshots, `criticalVisible`/`rootInViewport`/no-overflow). ⇒ This environment CAN run the app + acceptance machinery, so the §43–58 real-machine phases and the app-verified Scenario/Office removals are feasible. Remaining gates: `verify:reliability`, extend layout acceptance to the new workbench panels, resolution matrix across §50 viewports, packaged build + §56 re-verify.

## Verified increments log
- Batch 1–3 (Strategy live + presets + Task Plan model + Strategy→Plan chain): typecheck+lint+arch+design+ipc green; engine/research 28 tests.
- Batch 4 (`TaskPlanStore` persistence): engine/persistence tests green.
- Batch 5 (Strategy import/export/version store): ResearchStrategyStore tests green.
- Batch 6 (IPC + preload: `taskplan:*` ×7, `strategy:export/import/bumpVersion`; bootstrap domain): full typecheck green; **ipc snapshot baseline regenerated**; **preload API snapshot (`tests/electron/fixtures/metis-api-snapshot.json`) regenerated** via `METIS_UPDATE_API_SNAPSHOT=1`; arch + design gates green; 40 tests.
- Batch 7 (`TaskProgressPanel` live in ProjectsPage right column, real `taskplan:*` data): ProjectsPage 5/5 + TaskProgressPanel 3/3; full `vitest` **6120 passed / 9 skipped / 0 failed**; typecheck + lint green.
- Batch 8 (`CapabilityResolver` + `TaskExecutor`, §24 zero-injection proven through loop): 84/84 engine tests; arch gate green.
- Batch 9 (unified `UnifiedArtifact` contract + `UnifiedArtifactStore` + `ua:*` IPC/preload + live 阶段产出/交付成果 panel mounted in ProjectsPage — all 3 right sections now real-data): 16 affected tests; IPC + preload API snapshots regenerated; typecheck + lint + arch gate green (602 files, 0 cycles).
- Batch 10 (§18/§19 artifact rework: preview + edit→另存为新版本 (non-overwrite) + version chips + promote/delete in `ArtifactsPanel`, real `ua:*`): 5/5 panel tests; app typecheck + lint green.
- Batch 11 (§52 `gate:metis2-product` scoreboard, npm script): runnable, honest 4/9 PASS at head, exit 1 until all pass; ledger tracks it as the §57 driver.
- Batch 12 (§32 Task→Conversation 讨论 + §18 Artifact→Conversation 与AI讨论 via the real `chatIntent` seam — reinforces "Conversation 唯一入口"): 10/10 panel tests assert the intent carries projectId + task/artifact context; app typecheck + lint green.
- Batch 16 (§28 OmniRoute removal): deleted 10 OmniRoute files + trimmed ModelDiscoveryStore to MailboxPoolStore; snapshots regenerated; ProviderProfilesSection/IpcRegistryLifecycle/PreloadApiSnapshot updated; **full suite green (mail tests pass), product gate 5/9, `no-omniroute` PASS.**
- Batch 17 (§14 doc + branding): `docs/metis-office-removal-from-metis.md` (REMOVE/KEEP-internal/KEEP-standalone); removed "Metis Office" Settings branding; gate office check made faithful.
- Batch 18 (§22 Skills Center): `SettingsSkillsCenterSection.tsx` (real skill IPC: list/enable/copy/archive) mounted in Settings → `skill-center` PASS. **Full suite 6096/0, arch+design gates green, gate 6/9.**
- Batch 19 (§25 Prompt Engineering): 提示词工程 section over ArtifactPromptService + 4 new consumer-backed prompts fixing the empty-office-defaults defect (→ 8 wired prompts); gate `prompt-engineering` PASS. Registry 3/3, engine tsc/lint clean. **Gate 7/9.**
- Batch 20 (§6 文献准备 stage): `engine/literature/literatureLibrary.ts` — GB/T 7714 + APA citation formatter + `literature_library.md` assembly (all §6 fields) + artifact builder; wired via `ua:saveLiteratureLibrary` IPC (create/append-version). 6/6 + lifecycle 3/3; snapshots regenerated; electron typecheck clean. Retrieval itself stays the Conversation's job.
- Batch 21 (§38 final binding): legacy Scenario→Research Strategy merged into `listStrategies` via a scenario provider bound to `PersonalizationRepository.list('scenario')` (throw-contained). All three legacy types now surface in the new model. 7/7 store tests; engine+electron typecheck + lint clean.
- Batch 22 (§12 Reproducibility): `engine/research/reproducibility.ts` (project layout + `run_all.py` numeric-prefix ordering → stage=execution code artifact) consumed via `ua:saveRunAll` IPC; 5/5 + 19/19; snapshots regenerated; electron typecheck + lint clean.
- Electron runtime confirmed working here: `build:electron` PASS + `acceptance:layout` boots the app and passes (`"status": "passed"`) across the 5-viewport matrix. Probed adding a `settings` surface to the sweep — the Settings page navigates + renders (`rootInViewport: true`, no overflow), but the harness's single-above-fold critical model doesn't fit the long scrolling Settings page (研究策略/技能中心 render below the fold); experiment reverted, harness left green at HEAD. To sweep the new surfaces later, the harness needs a scroll-to-control step.
- Batch 24 (§11/§30 real execution loop wired end-to-end): `executeNextTaskForProject(projectId)` in `createDomains.ts` binds the injected `runModel`→`provider.complete([system=skillContext+acceptance, user=task])` and `saveArtifact`→`UnifiedArtifactStore` (`task:{id}` id, stage `execution`, version-append on rerun; §24 loads only enabled skill summaries, bodies reach the model only for resolver-selected skills). New `taskplan:execute` IPC channel + `taskPlanExecute` preload method; `TaskProgressPanel`「执行下一任务」now calls the real run (was a manual `start` flip) and surfaces COMPLETE / FAILED(model_not_configured→friendly msg, downstream reopened) — never a fake complete. Per-row buttons stay manual lifecycle overrides. IPC + preload API snapshots regenerated (`invoke=545 renderer=537`); electron+app typecheck, lint, arch gate (600 files/0 cycles) green; taskExecutionService/TaskExecutor/CapabilityResolver 15/15. **Live model run still pending a pinned free endpoint (§54) — a not-configured model yields FAILED, verified by the injected-seam tests.** Boundary locked by new `tests/electron/TaskPlanExecuteIpc.test.ts` (5/5).
- Batch 25 (§7 选题确认 vertical): `engine/topic/topicConfirmation.ts` — `TopicCandidate` + lossless `topic_<id>.md` render/parse (embedded machine block, prose-ignoring) + `buildTopicCandidateArtifact` (stage `topic`) + `confirmTopic`→unique `confirmed_topic.md` (empty/unknown→error, non-destructive, re-confirm versions it §20.1). Product-exposed via `ua:saveTopicCandidate`+`ua:confirmTopic` IPC (create-or-newVersion, matching the literature/run_all pattern) + preload; `ArtifactsPanel` gains「确认为选题」on topic-stage rows (incl. migrated legacy topics) + a 已确认选题 badge. 10/10 engine + 5/5 IPC (`tests/electron/TopicConfirmationIpc.test.ts`) + 15/15 panel/frontend tests; snapshots regenerated (`invoke=547 renderer=539`); typecheck(4)+lint+arch(601/0) green. Full-suite clean at prior head; native ABI note: `vitest run` alone needs `npm run rebuild:node` first (Electron build leaves `better_sqlite3.node` on ABI 145).
- Batch 26 (real-machine regression + §37 submission-from-deliverable): `npm run build:electron` PASS (typecheck + vite + electron-native rebuild) and `npm run acceptance:layout` **status: passed** across the 5-viewport matrix at head → my Batch 24/25 wiring launches + renders all four surfaces with no runtime/overflow regression. §37「登记投稿」: `ArtifactsPanel` now registers an **outcome-backed deliverable** (`legacy-outcome:<id>` from §38) as a submission case by calling the ALREADY-WIRED `createSubmissionCase`/`submission:createCase` bridge (strips prefix → `sourceOutcomeId`, carries `currentVersion` → `sourceOutcomeVersion`, sends title+journal; handles `duplicate_active` and null). No new IPC/methods/snapshot delta (pure UI entry point). Advances the submissions-nav-retirement prerequisite (submission now reachable from the workbench). 3 new `ArtifactsPanel.test.tsx` cases (9/9); lint + IPC snapshot unchanged. Native back to Electron ABI (145) → next `vitest run` needs `rebuild:node`.
- Batch 27 (§5 lifecycle staging in 阶段产出): `ArtifactsPanel` 阶段产出 now groups draft artifacts along the seven-stage §5 spine (文献→选题→设计→材料→执行→成果→交付) with per-stage headers + a trailing 其他产出 bucket for untagged ones (nothing hidden). Purely presentational (uses existing `artifact.stage`), model-free, no backend/IPC/CSS change → design-system + IPC snapshots unchanged. 1 new `ArtifactsPanel.test.tsx` case (10/10); app typecheck + lint green. Reinforces "阶段产出在右侧持续沉淀" + makes the §5 lifecycle visible in the workbench.
- Batch 28 (§10 stage-4 研究材料): `engine/research/researchMaterials.ts` — `ResearchMaterial` inventory model (8 kinds; each `ready` per §10「有则使用，没有则调用数据获取」) + `summarizeMaterials` (ready/missing/byKind/`theoreticalOnly`) + `materialsReady` stage gate + `renderResearchMaterialsMd` grouped checklist + `buildResearchMaterialsArtifact` (stage `materials`, id `research_materials`). Persisted via `ua:saveResearchMaterials` IPC (create-or-newVersion, mirrors §6/§12) + preload `uaSaveResearchMaterials`. 6/6 engine; electron typecheck+lint+arch(602/0) green; IPC (`invoke=548 renderer=540`) + preload API snapshots regenerated + `IpcRegistryLifecycle` ua count 10→11. Producer (upload/source search) stays the Conversation's job. This completes 4 of 7 §5 stages as first-class artifact primitives (literature/topic/materials/reproducibility→execution).
- Batch 29 (§23 skill-pack import/export): surfaced「导出技能包 / 导入技能包」toolbar in Settings 技能中心 (`SettingsSkillsCenterSection.tsx`), calling the ALREADY-WIRED `personalization:bundle:export`/`:import` bridge (`exportPersonalizationBundle`/`importPersonalizationBundle`; native save/open dialog + bundle integrity all handled in main). Export roots the bundle at the current skill definition ids (min 1, disabled when empty); import re-lists on success; `operationId` via `crypto.randomUUID` (already used in-repo). Pure component reuse — no new IPC channel/method → IPC + API snapshots unchanged, lifecycle count unchanged. 3 new `SettingsSkillsCenterSection.test.tsx` cases (6/6); app typecheck + lint green. Skill management now symmetric with strategy/prompt packs (§23 import/export done; conversational generation exists via `skill:generateFromConversation`/skillStudio, model-dependent).
- Batch 30 (§12 Research Strategy governs workbench plan generation): `TaskProgressPanel`「生成研究计划」now renders a per-project Research Strategy `<select>` fed by the existing `strategyList` IPC (presets + user strategies), passing the chosen `strategyId` to `taskplan:generate` (which already accepted it); the empty/"默认研究策略" option preserves the prior no-arg call. Makes §12 real from the workbench — the selected method shapes the generated RESEARCH_PLAN.md. Pure component reuse (no new IPC/method/CSS) → IPC + API snapshots + design-system unchanged. 1 new `TaskProgressPanel.test.tsx` case (5/5, incl. the preserved no-strategy `toHaveBeenCalledWith('p1')` path); app typecheck + lint green.
- Batch 31 (§21 Word export from the workbench): `ArtifactsPanel` detail「导出为 Word」for any text-bearing deliverable, reusing the already-wired `exportMarkdownAsDocx`/`outcomes:word:docx:exportMarkdown` bridge (`{title, markdown}` → native save dialog in main), no outcome-row coupling. 2 `ArtifactsPanel.test.tsx` cases (12/12); app typecheck + lint green; no IPC/snapshot delta. Closes part of the F1 outcomes parity gap.
- Batch 32 (§18/§19 soft-delete + workbench trash): `UnifiedArtifactStore` gains `archive`/`restore`/`getTrashed`/`listArchived`/`purge` — archived artifacts move to a SEPARATE persistence category `unified_artifact_trash` (live `list`/`get` unaffected, content retained not overwritten, §20.1), so a mistaken remove is recoverable until explicit purge. Exposed via 4 new `ua:*` IPC (`ua:archive`/`ua:listArchived`/`ua:restore`/`ua:purge`) + preload (`uaArchive`/`uaListArchived`/`uaRestore`/`uaPurge`); `ArtifactsPanel`「删除」→ soft「移除」+ a 回收站 section (恢复 / 彻底删除). This closes the F1 "trash/restore (ua:delete was hard)" parity gap — real data-safety improvement + workbench replacing OutcomesPage. Snapshots regenerated (`invoke=552 renderer=544`, preload API +4 methods); `IpcRegistryLifecycle` ua count 11→15. 4 store + 1 panel trash tests; **full suite 6157/0**, typecheck(4)+lint+arch(602/0) green.
- Batch 33 (§18 version compare): pure `diffLineCounts(from,to)` line-multiset diff added to `engine/artifacts/UnifiedArtifact.ts` (undefined/empty content = no lines, so an absent first version diffs cleanly); `ArtifactsPanel` detail shows a per-version「vN 较 vN-1：+A −B 行」badge. Closes the F1 "version compare" parity gap. No IPC/snapshot/CSS change (engine fn + display; the src→engine runtime import kept the graph acyclic — arch 602/0). 2 store + 1 panel tests (store 11/11, panel 14/14); full typecheck(4) green; IPC snapshot unchanged.
- Batch 34 (§26 per-task MCP loading wired into real execution): `planMcpIds` was dead output — now `executeNextTaskPersisted` computes the selected-skills' MCP set and threads it through `RunModelInput.loadedMcpIds` + `TaskExecutionOutcome.loadedMcpIds`; `executeNextTaskForProject` records it in the execution artifact's provenance (new optional `mcpIds` on `ArtifactProvenance`), the `taskplan:execute` IPC returns it, and `TaskProgressPanel`「执行下一任务」shows「按本任务加载技能 N、MCP M」. Turns clause-5's MCP half from unexercised into live + observable (server activation-by-id remains a documented follow-on — heavy consent/MCPManager integration, unverifiable without real servers). No new channel/method → IPC + preload API snapshots unchanged, arch 602/0. Strengthened `taskExecutionService.test.ts` with a `loadedMcpIds` assertion; **full suite 6160/0**.
- Batch 35 (§37/F3 submission-tracking visibility): `ArtifactsPanel` gained a read-only「投稿进度」collapsible section listing the project's submission cases (title · 目标期刊 · status) via the ALREADY-WIRED `listSubmissionCases` bridge — no new IPC/method/snapshot. F3 moves from "registration only (§37)" toward "lifecycle visible in the workbench". 1 `ArtifactsPanel.test.tsx` case (15/15) + ProjectsPage 5/5; app typecheck + lint green. F3 REMAINING: 选刊 shortlist, submission-assistant chat, portal browser, preflight/freeze/coverLetter ACTIONS (task #15).
- Batch 37 (§14 METIS Office product-entry removed → `no-office-product-entry` PASS, product gate 9/9): stripped the external-GenOffice editor affordance from `OutcomesPage.tsx` — removed the `useOutcomeGenoffice` import/call + destructure, the `outcomes-external-editor-actions` "Metis Office" section (launch/sync/discard), the always-false `nativeEmbeddedActive` embedded banner (:535), and its now-unneeded `!nativeEmbeddedActive` render guards (editors always render since the embedded path was already discontinued). §14.3 honored: internal Word/PPTX **generation** (OutcomeWord/Pptx services, GordenPpt, 导出为 Word) and the **standalone GenOffice app** are untouched — only the office *editor entry embedded in the METIS product path* is gone; the outcome external-editor IPC channels remain as orphaned-but-harmless surface. Stripped 2 stale "METIS Office" comment strings (SettingsOfficeProfilesSection:6, OfficePptRibbon:30) so brandingGone is truly clean; removed the now-unused `FileSpreadsheet` import. Replaced 4 office-flow tests in `OutcomesPage.test.tsx` with one asserting the entry is gone (intentional §14 behavior change). Verified: typecheck(4) green, eslint clean (1 non-fatal stale-disable warning), **full suite 6158/0**, `build:electron` PASS, `acceptance:layout` **passed**. Residual (low-priority, non-gating): dead `externalEditorSession` state + 2 unreachable "Metis Office" notices in OutcomesPage + orphaned useOutcomeGenoffice.ts/IPC — documented, not removed (out of §14's user-facing scope, extra risk).
- Batch 38 (§50/§55 multi-viewport acceptance): `VIEWPORT_MATRIX` extended to the spec's **6** viewports by adding 2560×1440 (`electron-layout-acceptance.py` + `RESPONSIVE_VIEWPORT_COUNT=6` in the runner). Ran `acceptance:layout` **twice consecutively** at 6 viewports × projects+scenes surfaces → both `"status": "passed"` (satisfies §50 viewport count + §55's two-consecutive-clean-rounds for the layout matrix). Honest scope note: §50's per-feature content checks (Settings / Skill Center / Prompt Engineering below-fold + long-name/filename/task-list overflow on a seeded project) and §55's full *feature-journey* sweep still need seeded-profile + live-model support in the harness; §56 packaged + CI + tree-clean remain. Also reconciled the §57 stop-conditions table (was all-stale TODO) against real evidence.
- Batch 39 (§33 partial — one clean Scenario DOM-entry removed, VERIFIED): `App.tsx` research-shell `plan` inspector now renders the real `<TaskProgressPanel projectId>` (gated on `activeResearchProjectId`, mirroring `ProjectsPage:769`) instead of `ScenarioLauncher` — deleting the last "打开场景 / Open Scenarios" CTA from a (write-mode) DOM path and upholding §30 "Task Plan 是唯一执行结构" in that shell. `ScenarioLauncher.tsx/.css` intentionally KEPT (deleting them breaks `docs/design-system-gate-baseline.json:64` which indexes the css; its own test renders the component directly and still passes). No new IPC/method → snapshots unchanged; no cycle. Verified: typecheck(4) green, `eslint src/App.tsx` clean, `gate:architecture` 602 files/0 cycles, scenario test set **102/102** (the 2 E2E fails were only the `rebuild:node` ABI gotcha, pass after rebuild). This is a genuinely *small* §33 slice — the full "Scenario 用户入口 = 0" flip remains TODO (see audit below).
- **Batch 40 (§33 COMPLETE — Scenario user entry retired, machine-verified):** `PersonalizationCenter` lands on the MCP extension library and the 场景 tab is removed; `ScenarioWorkbench` is now reachable only via a new internal `initialKind` prop (no user path — production passes nothing → 'mcp'), kept as legacy/superseded surface still covered by `ScenarioWorkbench.test.tsx` + a test-local shim so the ~54-test `PersonalizationCenter.test.tsx` suite drives it directly (avoids deleting coverage). Nav entry relabeled 场景→扩展 / Scenarios→Extensions (`personalization.title` + `nav.personalizationDesc`, id stays `personalization`); internal tab-nav aria-label 场景分类→扩展分类. 5 tab-dependent tests retargeted to assert the 场景 tab is *gone* / exercise tab-switch regression via the remaining MCP tab; `ScenarioDirtyGuard.test.tsx` (3-branch App-level scenario-leave, now an unreachable path) deleted with its discard→leave regression **ported** into `ScenarioWorkbench.test.tsx`; `PersonalizationHappyPathE2E` scenario-authoring case converted to a durable-skill round-trip (scenario→chat runtime stays covered by the engine runtime tests). Acceptance harness `scenes`→`extensions` surface + explicit `.scenario-workbench === 0` DOM assertion. **Verification:** typecheck(4) exit 0; `eslint` clean; `gate:architecture` 602 files/0 cycles; `gate:metis2-product` 9/9; `gate:design-system` PASS (`ScenarioLauncher.tsx/.css` intentionally retained so its baseline entry stays valid; App no longer renders it); **full `vitest` 6155/0** (was 6158 → −3 from the deleted App dirty-guard test); `build:electron` PASS + `acceptance:layout` top-level **passed** with `.scenario-workbench===0` across all 6 viewports (§55 layout matrix). NOTE (post-B39 audit correction): the retirement is NOT a "134-file cascade" when the legacy component is retained — the entry is retired by (a) landing+tab+launcher removal, (b) `initialKind`-only reachability, (c) machine-verified DOM absence; full deletion of `ScenarioWorkbench` + sub-components is now optional dead-code cleanup, not a §33 prerequisite.
- **Batch 41 (§33 coupled acceptance-tooling refresh — found via §33, also fixes B36 drift):** Retiring the 场景 entry surfaces that several CDP/packaged acceptance *scripts* (NOT covered by `vitest`) hard-coded the old nav/entry. Fixed the runnable gate consumers: (1) `tests/scripts/test_electron_safe_markdown_acceptance.py` — a meta-test that guards `electron-layout-acceptance.py`'s surface contract; it had been **stale since B36** (still asserted the 4-surface order `converse,projects,outcomes,scenes`) → retargeted to `('projects','extensions')` + `[projects,personalization]` + a new §33 assertion that no surface targets `scenario-workbench` and `scenes`/`outcomes` are gone; `pytest` **5/5**. (2) `scripts/agentc-pack-final-verify.cjs` §56 packaged check section 4 — clicked `personalization-trigger` and waited for `.scenario-workbench` → retargeted to the `personalization-library-trash-toggle` (extensions center) + explicit `.scenario-workbench===0` check; `node --check` OK. (3) `tests/scripts/test_run_electron_layout_acceptance.py` — the runner meta-test's `complete_report()` fixture still modeled the pre-B36 4-surface report AND the pre-2026-09-16 `rendererResponsiveMatrix:False` contract → aligned to the current `projects+extensions` / 6-viewport / `screenshots:12` contract (helper `activeSceneTab`→`activePersonalizationTab`, 2-surface mutations reindexed). **`python -m pytest tests/scripts/` now 12/12** (was 10 passed / 2 failed — RED since B36, masked by a green `vitest`). **REMAINING acceptance-phase tooling blocker (for §55/§56 execution):** `scripts/ui-feature-sweep.cjs`, `scripts/ui-resolution-matrix.cjs`, and the SAME packaged verifier still navigate demoted B36 destinations via top-nav (`clickNav('submissions'|'topics'|'outcomes'|'converse')`) and wait on `.scenario-workbench` — these run against the LIVE app via CDP (not part of `pytest tests/scripts` or `vitest`), so they need the §43-58 acceptance phase to be executed (run + fix each stale check against the live surfaces) to flip §55 full-sweep and §56 packaged.
- Batch 42 (§51 authoritative verification + §55 desktop first run — see §51 section above for the tier-2 detail): confirmed **Tier-1 `verify:reliability` = PASS 13/13** post-§33; corrected B41's overstatement (the tier manifest is authoritative; `scripts/ui-*.cjs` are non-gated probes). First Tier-2 desktop run after `build:electron`: **crash-relaunch-harness PASS** (earlier all-None was the Node-ABI/electron-app mismatch — environmental, NOT a defect / NOT §33), mcp-leak + child-process-leak PASS; **shutdown-relaunch real 10/11**, one failure (approved-scenario-result → `history_item_unavailable`) on chat-persistence files clean vs HEAD → pre-existing, out-of-convergence-scope, localized. No source changed.
- **Batch 43 (§17/§29 delivery-package actions surfaced in the right panel — headless-verified):** `ArtifactsPanel` 投稿案件行 add「预检」(→ `runSubmissionPreflight` → 阻塞/提示 汇总 + 逐项 level·label) and「生成并导出材料包」(→ `assembleSubmissionPackage`→`exportSubmissionPackage` → 导出目录 + 项数/失败数). Reuses existing submission IPC (**no new channel/method → ipc/preload snapshots unchanged**); discriminated-union `res.ok` narrowing tsc-clean; pure UI, model-free. 2 `ArtifactsPanel.test.tsx` cases (17/17). Verified: eslint clean, typecheck(4), arch 602/0, product 9/9, **full vitest 6157/0** (+2). §57 line 85 Delivery → package preflight + assemble/export now in the right panel (live real-case/model + §37 选刊/助手/portal remain).
- **Batch 44 (§17/§29/§37 delivery-package lifecycle completed in the right panel — headless-verified):** `ArtifactsPanel` 投稿案件行 add「校验材料包」(`getSubmissionPackage`→`validateSubmissionPackage` → 有效/无效/待确认/待定 汇总) and「冻结材料包」(`getSubmissionPackage`→`freezeSubmissionPackage` → 已冻结徽章；未过预检时 surfaces `blockers` 逐项标签)。复用既有 submission IPC，**无新 channel/method**；model-free。2 `ArtifactsPanel.test.tsx` 用例（19/19：校验/冻结链带 `{projectId,packageId}`；冻结阻塞显示「无法冻结：缺少 Cover Letter」）。Fixed an indexed-access narrowing TS error by binding `pf/val/frozen` locals in the map body. **Confirmed gates:** tsc(4) exit 0, eslint clean, arch 602/0, product 9/9, ArtifactsPanel 19/19, ProjectsPage 5/5; **full `vitest` 6159/0** (B43 6157 + B44's 2; §51 full-suite confirmation of the delivery additions). Real-machine re-verified: `build:electron` + `acceptance:layout` **status:passed** at this head (delivery buttons behind a default-off toggle → `.projects-page` surface unchanged; `.scenario-workbench===0` still holds across 6 viewports).
- Batch 45 (§51 full-suite close + §56 packaged pipeline + convergence-integrity audit + §55 root-cause): full `vitest` **6159/0** (the backgrounded chained run completed exit 0). §56: `npm run pack` (`electron-builder --dir`) **exit 0 at head** → `release/win-unpacked/…exe` + genoffice staged + signed (line 89 TODO→PARTIAL); noted `npmRebuild:false` bakes current ABI so a runnable smoke needs `rebuild:electron`-then-pack, and `agentc-pack-final-verify.cjs` still carries B36 nav (`clickNav('topics'|'submissions')`) + an external `example.com` check → full §56 re-verify is a scoped verifier migration. Convergence-integrity audit: `RESEARCH_NAV_ITEMS = getPrimaryResearchNav() = []` ⇒ App.tsx's `.filter('topics'|'outcomes'|'submissions')` blocks render nothing — §34-36 holds genuinely, not a proxy. §55 shutdown-relaunch root-caused (not blind-fixed): `decodeStoredHistoryEntry` emits `history_item_unavailable` when `StoredHistoryRowSchema` (strict union) rejects the approved-scenario **result** row — chat-runtime writer clean vs HEAD → pre-existing, out-of-convergence-scope.
- **Batch 46 (§57 line 77 — deterministic engine coverage of the Task Plan execution state machine):** new `tests/engine/taskExecutionService.test.ts` (4/4) exercises `executeNextTaskPersisted` directly with injected `runModel`/`saveArtifact` (no live model, no IPC): Execute→Verify→Save→**COMPLETE** + artifact persisted + task state COMPLETE + §24 zero-injection io contract (`skillContext` string, `loadedSkillIds`/`loadedMcpIds` arrays); model-declines → **FAILED** (no artifact, not COMPLETE); ok-no-output → COMPLETE w/o artifact; no-runnable → model untouched. Closes a real gap: the engine fn had ZERO tests despite its "unit-testable without a live model" docstring (only the IPC envelope was covered). Test-only (no src/IPC/snapshot change); eslint clean, typecheck(4) 0.
- **Batch 47 (§9 Research-Strategy→Task-Plan influence now proven deterministically):** new `tests/engine/taskPlanGenerator.test.ts` (3/3) — resolves the long-standing "strategy→Task-Plan influence pending" caveat. Asserts `generateTaskPlanFromStrategy` yields one dependency-chained task per strategy phase (title=phase.name; section from `ACTION_SECTION[phase.action]`; acceptance from `phase.prompt` else verifiable fallback; derived states first=READY/downstream=TODO), that two preset strategies produce materially different task sets (not decorative), and `planCoversStrategyMoves` confirms phases land in the plan. Corrected a `deriveStates` expectation during authoring (downstream of a fresh all-TODO plan is TODO, not BLOCKED — BLOCKED requires a FAILED/BLOCKED predecessor, TaskPlan.ts:212). Test-only; eslint clean.
- **Batch 48 (§24/§26 dynamic-loading guarantees now directly tested):** new `tests/engine/CapabilityResolver.test.ts` (4/4) — `CapabilityResolver` had ZERO direct tests; only exercised indirectly via the execution service. Locks the objective's "Skill/MCP 按当前任务动态加载" hard constraint: `buildSkillContext` emits ONLY selected (incl. `required` Strategy Core) bodies and NEVER an excluded skill's body (§24 zero-injection, asserted via an absent marker); `planMcpIds` returns only the selected skills' MCP servers (§26 on-demand, excludes the non-selected server); title-hit scores above description-hit. Test-only; eslint clean, typecheck(4) 0. Tasks #3/#4/#8 closed with these deterministic engine/generator/resolver tests. §57 line 85: 投稿材料包生命周期 preflight→组装/导出→校验→冻结 now live in the right panel; 最终提交 (`confirmFinalSubmission`, Human-Approval 门控 + status machine) deliberately NOT surfaced blind — needs an approval UI, kept as documented next step to avoid an under-tested submit affordance.

- **Batch 49 (§55 Tier-2 blocker CLOSED — three real P1 data-loss defects, all reproduced before fixing; `acceptance-shutdown-relaunch` now PASSES on real Electron):**
  - **(1) Scenario step-card messages vanished on reopen.** `HistoryRunMetadataSchema` is a `z.strictObject`, but `ScenarioWorkflowService.ts:1353/1587` writes `metadata.stepCard`, which `useChatHistoryRestore.ts:159` legitimately reads back to rebuild the card. The strict row was rejected → `decodeStoredHistoryEntry` returned `history_item_unavailable` → the assistant message was gone. Fix: bounded `ScenarioStepCardSchema` added to the metadata contract — top level stays strict (unknown *message* keys are still a violation) but nested card display data is lenient, so adding a card field can never again delete a user's message. `tests/engine/ChatRuntimeContract.test.ts` +3 (incl. a real-SQLite round trip through `AgentChatHost.messagesGet`'s exact projection).
  - **(2) Generated artifacts were destroyed by their own project mirror (mainline P1).** `ChatPage.tsx:234` / `App.tsx:678` / `AgentChatHost.ts:410` write the sentinel `'global'` as `sessions.project_id`; `createArtifacts` mirrored that verbatim into `research_artifacts`, whose FK to `projects` has no `'global'` row → `FOREIGN KEY constraint failed` inside the same transaction → **the artifact insert rolled back too**, surfacing as "生成物注册失败，请检查存储". `deleteSession` had the identical hazard (a 'global' conversation holding artifacts could not be deleted at all). Fix: `resolveArtifactOwnerProject` — a dangling owner id is collected by `UNASSIGNED_ARTIFACT_PROJECT_ID`, a genuinely project-less session still defers as documented. Proven RED first (same `SqliteError` text as production), then GREEN; `tests/engine/Persistence.test.ts` +3 including a guard that a REAL project still mirrors to that project (so the fix can't be an accidental bypass).
  - **(3) A successful save was reported as a storage failure.** `mirrorScenarioArtifactToResearch` threw for project-less chats *inside the same try* as `createArtifacts`, so a committed artifact still returned `null`. Made best-effort (log, never block) at the single helper — all three call sites — matching this file's own documented literature-bridge policy.
  - **Gate honesty:** the e2e assertion demanded the marker in the chat bubble although production deliberately persists full output as a 生成物 and only a summary in chat (documented at `ScenarioWorkflowService.ts:1555`). Retargeted to what the gate names: history rows decode with **no** recovery placeholders, AND the approved text is read back from the persisted artifact via real IPC. Evidence: `artifactCount 2`, `persistedChars 34`, marker present → phase-2 `passed`, top-level `E2E_EXIT=0`, 9/9 outer assertions PASS. No assertion was removed; one was replaced by two stronger ones.
  - **§57 P0/P1 sweep started (task #18):** an audit of every "parse-and-fall-back-silently" contract reader found a second live instance of the same class — **`paper:list` returned `[]` for any non-empty library**: `getPapers()` always emits `referenceIds` (`PersistenceStore.ts:812`), the renderer view schema omits it, and `decodeLibraryPaperList` collapsed to `[]` if ANY row failed. `presentPaper` now strips it (the renderer's IPC row type never had it and re-derives it locally), and all three library list decoders validate per-row so one bad row can't empty a collection. `tests/electron/LibraryPaperRoundTrip.test.ts` (4/4, NEW) is the first test to feed the real writer's rows through the real projection; `presentPaper` had **zero** prior coverage. Remaining candidates logged as latent, not blind-fixed: goal-card rows (`GoalCardData.pauseRequested` ∉ strict `GoalSnapshotSchema` — verified latent today because both writers build fresh literals without it), `ResearchStrategyStore.ts:31` silent `parse` drops, and the all-or-nothing session/experiment list decoders.
  - **Correction to the sweep bullet above — a fix was attempted, then REVERTED on evidence.** The same batch initially also made the three library list decoders per-row tolerant. A full-suite run then failed `preload.test.ts:983 · "paper:list IPC rejects entire array when any paper has pdfPath"`, which shows that all-or-nothing decoding is a **deliberate fail-closed privacy tripwire**: a local `pdfPath` must never reach the renderer, and the test asserts the string `private` cannot appear in the payload. Partial emission is the outcome to avoid, not the empty one. So the tripwire was load-bearing and my change weakened it → **reverted**; the empty library was caused solely by the projection letting a store-only field through, and that is fixed at the correct layer. `LibraryPaperRoundTrip.test.ts` now asserts **both** directions (real writer rows decode; one unacceptable row still empties the payload without leaking the path), and `LibraryRuntimeContract` documents the tripwire so it is not "fixed" again. **⇒ Lesson: a silent `[]` is not automatically a defect to widen — first check whether the strictness IS the security boundary.** Remaining sweep candidates stay unfixed pending their own reproduce-first cycle: goal-card rows (`GoalCardData.pauseRequested` ∉ strict `GoalSnapshotSchema`; verified latent today, since both writers build fresh literals without it) and `ResearchStrategyStore.ts:31` silent `parse` drops.
  - **Open verification item (not yet claimed as green):** the full-suite run for this batch never produced an authoritative total. Under ~39 leaked `node.exe` vitest forks from a wedged earlier run, the MCP-heavy suites failed on **timeouts** (8.1s–105.8s per test, e.g. `spawns and stops 20 real MCP stdio servers and reaps every child`, `rejects disabled, undeclared, revision-drifted and disallowed definitions fail-closed`) and `pages.test.tsx` / `App.test.tsx` failed with `Test timed out in 5000ms` — the latter shown to be load flakes (131/131 green at `--testTimeout=25000`). Those MCP files have NOT yet been re-verified in a quiet environment; do not record §51 full-suite as passing for Batch 49 until they are.
  - **Follow-up evidence (same day):** the run totalled **6020 passed / 64 failed / 14 files failed**, and the failure mix was profiled rather than excused: **36 × `Test timed out`**, plus real-child-process casualties (`Real MCP probe failed: actual_probe_failed` ×6, `Actual probe failed` ×4, leak-gate `Request timeout: initialize`), and jsdom render-wait misses (`chat-page` ×5, trash-toggle ×1). The only non-timeout-looking assertions (`PersonalizationMcpActivationService.test.ts` ×3, "promise resolved instead of rejecting") were then run **in isolation: 14/14 green**. Root cause of the starvation was measured, not assumed: `Get-CimInstance Win32_Process` showed ~34 live `node.exe` processes belonging to **the user's other projects** (`D:\METIS_PARALLEL\A_core|C_products|integration` vite/tsx/dev-server pairs from 09-19 → 09-24) plus CloudBase MCP / Qoder computer-control / workbuddy processes — only 2 were mine and both had already exited. They were deliberately NOT killed. ⇒ §51 full-suite for Batch 49 is **unverified under contention**, not failing; re-run needs a quiet machine.
  - **Post-revert confirmation (final state of Batch 49):** `npm run typecheck` **exit 0** (all 4 tsconfigs) · `eslint` on every touched file **exit 0** · `ChatRuntimeContract` + `Persistence` + `LibraryPaperRoundTrip` + `ScenarioWorkflowService` **78/78 green**, i.e. the four new persistence/contract suites AND the security tripwire test pass together.
  - Verified this batch: `build:electron` **exit 0** (⇒ all 4 tsconfigs green with these changes) · relaunch e2e **passed** · targeted suites green (contract 24/24, persistence 23/23, library round-trip 4/4, scenario+chat 67/67) · fresh **typecheck clean (0 errors)** · **`eslint .` 0 errors** / 36 pre-existing warnings (the first pass caught 3 real `TS6133` dead-code errors my refactor left behind — removed, not baselined) · `gate:architecture` and `gate:design-system` **exit 0**.
  - **Flake discipline note:** the first full-suite pass reported `pages.test.tsx` (2) + `App.test.tsx` (1) failures. They are **`Test timed out in 5000ms`**, not assertion failures, and reproduce only under load: ~39 leaked `node.exe` vitest forks from a wedged earlier run were competing for CPU, and `pages.test.tsx` then ran **131/131 green with `--testTimeout=25000`** (its 131 tests take ~35s, so some legitimately cross 5s). Not attributed to these code changes without evidence, and no `node.exe` was mass-killed (that would kill the user's MCP servers / this CLI). Full-suite re-run with headroom is the authoritative §51 number.

- **Batch 53 (§57 P0/P1 sweep — 5th live silent-data-loss defect, this one in the §38 compat layer):** `strategy:list` was **empty for every user who has any legacy scenario**. `scenarioToStrategy` (§38's legacy-Scenario → Research Strategy projection) declares `ResearchStrategy` but never checked that contract, while `strategy:list` returns `listStrategies()` **unvalidated** (`registerStrategyIpc.ts:48`) and the preload decodes it with a strict `decodeStrategyListResponse` that answers ANY rejected row with `{ ok: false, strategies: [] }` — the `paper:list` shape again, so one invalid built-in hid the presets and the user's own saved strategies alike. The certain trigger: `PersonalizationIdSchema` deliberately allows `/` in ids (`user:scenarios/<slug>`, see the comment at `PersonalizationRuntimeContract.ts:82`), but `ResearchStrategy.id` is `RuntimeIdSchema`, which does **not** → `legacy-scenario:user:scenarios/x` is always contract-invalid. Two more overflow paths found by reading the schema against the projection: `phases` caps at **32** while `workflow` mapped 1:1, and phase/strategy `name` is `.trim().min(1)` while `step.name` passed through raw. Reproduced first: `tests/engine/LegacyScenarioStrategyRoundTrip.test.ts` (3/3, all RED before the fix) covers the id shape, the 40-step workflow, the blank step name, and the blast radius. Fixed at the right layer — the projection now actually satisfies the type it claims (deterministic id sanitization so the same scenario always maps to the same strategy id, name/description/prompt clamped to `RESEARCH_STRATEGY_LIMITS`, leading phases kept on truncation) — plus `containValidStrategies` in `listStrategies` so a future producer can't repeat the whole-list outage, warning rather than dropping silently. Note the deliberate contrast with B51: here per-row containment is correct because nothing is being leaked; in the library case the all-or-nothing decode **was** the `pdfPath` privacy tripwire and had to stay. Regression check: `vitest run strategy` → **6 files / 40 tests green** (projection, store, pack import/export, IPC untouched).
  - **Batch 53b — 6th defect, and a third failure mode.** `GoalSnapshotSchema`'s phase enum omitted `'paused'`, which `goalStatusToCardPhase` (`src/pages/chat/chatPageModel.ts:121`) really produces, and `GoalCardData.pauseRequested` was likewise absent from the strict snapshot. Reproduced via a real `appendMessage('goal', '__GOAL_CARD__…') → getMessages → decodeStoredHistory` round trip (`tests/engine/ChatRuntimeContract.test.ts`, RED then GREEN): the row did **not** error — `normalizeKnownValue` quietly rewrote the phase to `'unknown'`, so a paused Goal reopens with its state downgraded and no diagnostic anywhere, while the extra `pauseRequested` key rejects the whole row. That is the third distinct silence mechanism in this class, after whole-collection collapse (B49/`paper:list`/`strategy:list`) and row loss. Fixed by admitting both fields, with a companion test asserting the `stepStatuses`↔`steps` superRefine still rejects an orphan status — so widening the schema did not cost the invariant (same discipline as B51's tripwire reversal). `vitest run <contract> goal` → 6 files / 91 tests green.

- **Batch 54 (sweep closed + a scoreboard row corrected):**
  - **Task #18 completed.** The two remaining candidates were verified rather than assumed. **Session list:** `decodeLegacySessionList` does empty the whole conversation list on one bad row, but `decodeLegacySessionRecord` rebuilds an explicit presentation object (`selectLegacyMetadata` + hand-picked fields), so producer-only columns can neither break it nor leak; the fail-closed there guards malformed data, not a `pdfPath`-style secret — so it was left alone and pinned instead by `tests/engine/SessionListRoundTrip.test.ts` (4/4): real writer rows all survive, undeclared metadata never crosses the boundary, the recovery path still fires on a structurally broken row, and the versioned response decodes. **Experiment list:** already per-row tolerant, its `status` enum has no `'unknown'` normalization (drift rejects instead of downgrading), and the store emits only schema keys. Six defects fixed total; three silence mechanisms now documented (collection collapse, row loss, silent enum downgrade).
  - **§57 "Skill Creation" was mis-scoped and is corrected** (see that row): `/skill` is already a first-class composer command with a full implementation, and the learn-skill button was **deliberately removed by the 2026-09 spec** — a test asserts its absence. The planned button would have reverted that decision; the genuinely missing piece was coverage, so 2 tests were added proving composer input → `chatTurnFlow` → preload bridge → installed-skill report, plus the too-short guard. `ChatPageStreaming` **12/12**. Lesson recorded in memory: check for an existing affordance and for a test that pins an intentional removal *before* building.
  - Also this batch: verified typecheck/lint clean for B53/B53b (typecheck 0, eslint 0, 91 contract+goal tests) and restored the Node ABI after the packaged Electron runs so `vitest` remains runnable.

- **Batch 55 (§51 authoritative test count finally established + §58 report delivered):** the full suite was re-run once the machine went quiet (3 `node.exe` instead of ~34 — the earlier processes belonged to the user's *other* projects under `D:\METIS_PARALLEL`, and were deliberately not killed): **`vitest run` → 6191 passed / 9 skipped / 0 failed, 579 files, exit 0**. This closes B49's open item ("full-suite for Batch 49 unverified under contention"). Cross-check: 6170 at B48 + exactly the 21 tests added across B49–B54 (3 history contract, 3 persistence ownership, 4 paper round-trip, 2 goal card, 3 strategy round-trip, 4 session list, 2 `/skill`) = 6191, so nothing was deleted or skipped to buy green. Delivered the missing §58 artifact `docs/metis2-final-report.md` (20 mandated sections; verdicts limited to PASS/FAIL/NOT RUN/BLOCKED; `START_SHA` = `END_SHA` = `d03921e` because the tree is uncommitted; 10 open items listed rather than rounded off). Two spec readings were corrected against the source rather than the ledger: **§16 wants PPT as a Skill-driven deliverable** (`engine/skills/PptDeckSkill.ts`, auto-mounted on PPT intent, 21 templates, no branding) — not a hand-written .pptx writer, and no Office/LibreOffice validator exists on this machine to prove one anyway; and **§57's "Skill Creation not surfaced" was simply wrong** (`/skill` is a first-class composer command; the button was intentionally removed by a test-pinned spec decision), so the gap was coverage, not capability.

- **Batch 56 (§55 satisfied — but only after discovering the acceptance harnesses still described the pre-convergence product):** `phase2-full-sweep` rounds A and B were run consecutively on the current tree and both returned **7/7 steps, exit 0**, with identical numbers: `ui-feature-sweep` 29/29, `ui-resolution-matrix` 106/106 across all 6 viewports, `channel-probe`, `acceptance:layout`, `gate:architecture`, `gate:design-system`, `ui-baseline-capture` 49 shots / 0 warnings.
  - **Why this was blocked and what was actually wrong:** round A scored 4/7, and all three failures had one root cause — the harnesses clicked `topics` / `outcomes` / `submissions` (which now throw `missing nav entry`, since `getPrimaryResearchNav()` returns only `personalization`) and waited for `[data-testid="scenario-workbench"]`, retired by §33. The historical "two consecutive clean rounds" at `d03921e` therefore measured the OLD information architecture. Fixed the scripts' assumptions, not the app: topbar expectation corrected to the three real entries with the retired ids folded into the same measured geometry assertion (a resurrected entry now fails); surfaces retargeted to the contract `acceptance:layout` migrated to in B40/B41; retired-page steps replaced with converged forms (§33 absence, extensions opening on the skill/MCP library, topic/outcome capabilities asserted through their real IPC from the workbench).
  - **Two claims of mine were wrong and are retracted:** that §55 "needs seeded profiles + a live model" (the orchestrator starts its own loopback provider), and my read of the `check(..., true)` lines as fake passes (they follow `waitForDom`/`clickNav`, which throw — so the harness failed loudly rather than passing falsely).
  - **Disclosed rather than smoothed:** matrix assertions 196→106 because three surfaces described pages that no longer exist (no assertions kept for absent UI, none deleted for present UI); the project-scoped right-column mount is recorded `NOT RUN` with an evidence probe, because the sweep creates projects through main-process IPC and the renderer store never refreshes. Two of my own harness bugs surfaced during this: assigning `report.evidence.*` in `ui-baseline-capture` (that child report has no `evidence` bag → threw and aborted two viewports mid-capture) and a missing `warnings: []` initializer in `ui-feature-sweep`; both fixed, `eslint` clean on all three scripts. No test depends on these scripts, so the 6191/0 suite result stands; Node ABI restored for dev/test.

- **Batch 57 (the objective's last clause is now verified on real Electron, not just in jsdom):** extended `ui-feature-sweep` to drive the product's **real** create-project flow (sidebar 新建项目 → title → submit; the directory field is optional — `ProjectsPage.tsx:515` gates submit only on a non-empty title) and then assert the converged right column. Measured: `task-progress-panel` **and** `artifacts-panel` mount for the active project, the strategy picker is exposed, clicking generate yields **4 task rows** whose state badges read `["可执行","待办","待办","待办"]` — exactly §30's derivation (first task READY, downstream locked) — `taskplan-run-next` becomes available, and every task carries a `task-discuss-*` entry back into the conversation (§32); the submission and trash affordances are present in the same panel. This retires the `NOT RUN` B56 had recorded, and it is the strongest real-machine evidence yet for "Task Plan 是唯一执行结构" + "成果在右侧持续沉淀和再加工". Sweep grew **29 → 37 assertions**; both §55 rounds were **re-run afterwards** and pass 7/7 with exit 0 each, so §55 is not claimed against a stale harness.
  - Two self-inflicted errors found and fixed while doing this, both worth remembering: I ran the Electron-launching sweep right after restoring the **Node** ABI (→ `first-run provider save activates runtime` failed at 2/3, because SQLite needs the Electron build for the app under test — the known ABI trap, self-inflicted), and my first create-form step **toggled the form closed** because sweep step 1 already opens it (→ made it idempotent). `eslint` clean on the changed script; Node ABI restored afterwards so `npm test`/vitest keeps working.

- **Batch 58 (§11/§29 real execution verified — and it exposed a genuine product defect, since fixed):** extended `ui-feature-sweep` past plan generation to actually press **run-next** and require the output to appear as a 阶段产出 row. First run: the task reached `完成` and the downstream badge correctly flipped to `可执行` (§30 re-derivation confirmed live), **but the artifact row count stayed 0** — and that was not a test artifact. `ArtifactsPanel` loads only in a `useEffect` keyed on `[projectId]` with no subscription to unified-artifact creation, so an output saved into the **currently active** project never appeared until the user switched projects. In other words "阶段产出在右侧持续沉淀" was broken on exactly the path the objective cares about, and it had passed unnoticed because every existing test either remounted the panel or asserted the store directly.
  - Fix: `taskplan:execute` already reports `execute.artifactRef`, so `TaskProgressPanel` now takes `onArtifactsChanged?` and calls it on `COMPLETE` with an artifact; `ProjectsPage` (which owns both siblings) bumps an `artifactRefresh` counter passed to `ArtifactsPanel` as the new `refreshKey` prop added to its load deps. No new IPC channel, no cross-process event, nothing refreshed speculatively — a `FAILED` execution must not trigger a reload, and that is asserted.
  - Verified end to end: `ui-feature-sweep` **40/40** with `taskExecution: {rowsBefore: 0, rows: 1, badges: ["完成","可执行","待办","待办"], waitedMs ≈ 1010}`, and **§55 rounds A and B were both re-run after the source change: 7/7 steps, exit 0, identical evidence in both**. jsdom guard: 2 new `TaskProgressPanel` tests (COMPLETE→notified once; FAILED→not notified), file now 7/7.
  - Discipline note: the first sweep version broke its wait on the badge alone, which would have mis-reported a passing product as broken; waiting specifically on the row count is what distinguished "not saved" from "saved but not displayed" and led to the real fix. Also re-struck the ABI trap (ran the Electron-launching sweep right after `rebuild:node` → `first-run provider save activates runtime` failed at 2/3); ordering is now build:electron → sweep rounds → rebuild:node → vitest.

- **Batch 59 (§5's second half asserted live):** B58 proved the executed output *lands* in the right panel; it did not prove it is *filed*. §5 is a seven-stage lifecycle spine, and `taskplan:execute` stamps its artifact `stage: 'execution'` (`electron/bootstrap/createDomains.ts:316`), so the correct real-machine outcome is a row inside 「⑤ 分析执行」 — a row in `其他产出` or outside any group would mean storage without lifecycle presentation. `ui-feature-sweep` now captures each artifact row together with its owning `stage-group-*` container and title, and asserts it: **41/41** measured `{"row":"artifact-row-task:T001","group":"stage-group-execution","title":"⑤ 分析执行"}`, badges `["完成","可执行","待办","待办"]`, rows `0 → 1`.
  - Falsifiability was proven the same way as for product defects, not assumed: a temporary copy of the harness with the expected group changed to `stage-group-topic` scored **40/41**, failing exactly this assertion while still printing the real group — so the green run is a measurement, not a tautology. The temp copy was deleted; `grep TEMP-RED-CHECK` is clean repo-wide.
  - **§55 re-run on the strengthened harness, two consecutive rounds:** A and B each **7/7 steps, exit 0** (144 s apiece, finished 07:46:16Z / 07:48:40Z), sweep **41/41**, matrix **106/106** over 6 viewports, baseline capture **49 shots / 0 warnings**, and byte-identical execution evidence in both rounds. Node ABI restored afterwards (`rebuild:node` → `check-host-abi`) so vitest keeps working.

- **Batch 60 (the campaign's own gates were not actually in the PR gate):** `build/reliability-tiers.json` is §51's authoritative scoreboard, and `nightly.yml` / `release.yml` both enforce *tier 1* — which did **not** contain `gate:metis2-product`, `gate:architecture` or `gate:design-system`. A change that re-introduced a retired product surface, an import cycle, or a design-system violation could therefore pass PR, nightly and the release trigger. Added `gate-metis2-product` / `gate-architecture` / `gate-design-system` to tier 1 (**13 → 16** checks; JSON re-parsed, no duplicate ids) and ran them through the authoritative executor: `reliability-gate.mjs --tier=1 --only=gate-metis2-product,gate-architecture,gate-design-system` → **`gate: PASS (passed=3 failed=0)`** (1.0 s / 1.5 s / 0.9 s).
  - `gate:metis2-product` also grew from 9 to **11** checks. §52 says "至少检查" (at least), and two clauses of the objective had *no* gate coverage at all: **Research Strategy 负责研究方法** and **Skill/MCP 按当前任务动态加载**. New `strategy-drives-task-plan` requires the IPC to call `generateTaskPlanFromStrategy` and the right panel to expose the strategy picker + generate entry; new `per-task-capability-loading` requires `resolveCapabilities(task.title, …)` (per-task, not global) and the host to hand the model only `loadedSkillIds`. Both verified **11/11**, and each was mutated in a throwaway copy to confirm it fails alone (10/11 with the right check red, twice).
  - `ci.yml`: dropped `tests/docs` from the `engine-integration-security` matrix group — that directory has never existed (`git log -- tests/docs` is empty), and because vitest positionals are OR-ed filters it was a silent dud rather than a failure. Confirmed the remaining **10** distinct paths each contain real `*.test.ts(x)` files, added a `convergence-gates` job (checkout → npm ci → the three gates, ~3.5 s locally), and validated the YAML with a parser (jobs: quality, convergence-gates, security-report, tests, build). Every command referenced by `ci.yml` / `nightly.yml` / `release.yml` now resolves to a real script (`verify:reliability`, `verify:reliability:nightly`, `release:windows`, `scripts/install-upgrade-smoke.cjs`).
  - **What this does NOT buy:** no GitHub Actions run exists, so §17 stays `NOT RUN`; execution needs the commit/push authorization that also gates tier-4.
- **Batch 61 (§20 图表重画 — the spec's own P0 feature — implemented and wired, pixels still provider-gated):** a grep for `redraw|图表重画|figureRedraw` across `engine/`, `electron/`, `src/`, `tests/` returned **nothing**: §20 had never been built. The only part that existed was §20.1's version chain, and the right panel actively blocked re-working a figure (`ArtifactsPanel.tsx:440` disables 另存为新版本 for image kinds), so "阶段产出…再加工" was false for exactly the artifact type §20 is about. Built on top of capabilities that already existed rather than as a new subsystem:
  - `engine/research/figureRedraw.ts` (pure): what may be redrawn (must be kind `image`/`figure` **and** outcome-backed, because the outcome's managed media area is the only sanctioned place generated images land); the §20 request builder, which carries the artifact's own current definition + version + `provenance.source` + strategy + task into `visualContext` and omits any fact that is absent instead of inventing one; §20.1's write plan (`planFigureRedrawWrite`) — a legacy projection is first materialized as v1 (identical to what the user was shown) and the redraw appends as v2, a native figure just appends.
  - `redrawFigureArtifact()` in `createDomains.ts`: §24 resolution over the **instruction** (`resolveCapabilities` → `buildSkillContext`, so only hit skills reach the prompt) → the existing `OutcomeImageService.generate` → appended version referencing `outcome-media:<outcomeId>/<mediaId>`. A declined draw (no provider, HTTP error, bad response, media persist failure) returns that code and writes nothing.
  - `ua:redrawFigure` + `uaRedrawFigure` + the three contract baselines regenerated honestly (ipc-inventory `invoke` 552→553, preload method snapshot, `IpcRegistryLifecycle` ua count 15→16) — `ipc:snapshot:check` afterwards reports no drift. `docs/architecture-phase2-ipc-inventory.md` regenerated (549 unique invoke channels).
  - `ArtifactsPanel`: 「重画这张图」+ §20's seven example instructions as one-tap chips + instruction field + status note; preview for a media reference resolves through `readOutcomeMedia` (the renderer never reads files by path), and shows an honest "加载中/未通过完整性校验" line rather than a broken `<img>` when it cannot.
  - Tests: `engine/research/figureRedraw.test.ts` **11/11** (incl. the version chain on **real SQLite**: v1 content untouched, `parentVersion=1`, second redraw → `[1,2,3]`; and the composed prompt validated against the **real** `OutcomeImageGenerateRequestSchema`), `tests/electron/UnifiedArtifactRedrawIpc.test.ts` **4/4** (real registrar + real store: appended version returned, declined draw writes nothing, malformed args and foreign sender never reach the backend), `tests/frontend/ArtifactsPanel.test.tsx` **25/25** (+6). §52 gate grew a `figure-redraw-available` check → **12/12**, mutation-verified (a bogus channel name flips only that row).
  - **Two defects found by writing the tests rather than reading the code:** (1) `buildFigureRedrawRequest` originally only included the artifact's definition if a caller remembered to pass it — the definition is the artifact's own current content, so it is now carried by default (the test caught the omission); (2) the image request contract caps `prompt` at 8 000 chars (`strictObject`, so an over-long composition would have come back as an unexplained `invalid_request` once any user installed a long drawing skill) — `composeDrawingPrompt` now trims the skill context around the user's ask, and the first version of it was itself off by one (8 001), which the schema-against-the-real-consumer test caught.
  - **Final verification pass on the B61 tree, in dependency order:** `typecheck`(4 tsconfigs) exit 0 → `eslint .` 0 errors (36 pre-existing warnings; one new error — a synchronous `setState` inside my preview effect, fixed by deriving instead of disabling the rule) → `gate:metis2-product` 12/12 → `gate:architecture` 602/0 → `gate:design-system` PASS → `ipc:snapshot:check` no drift → `npm run build:electron` exit 0 → **§55 rounds A (08:46:19Z) and B (08:48:46Z): 7/7 steps exit 0 each**, sweep 41/41, matrix 106/106, baseline 49 shots / 0 warnings, spine evidence identical → `npm run pack` exit 0 + packaged CDP **`pack-final verify: PASS (35/35) captures=6`** (so §56 is re-proven on this tree, not inherited from B52; the bridge-set comparison also confirms `uaRedrawFigure` is really in the exe) → `rebuild:node` → **full `vitest run` 6215 passed / 0 failed / 9 skipped (6224), 581 files** (6193 at B58 + 1 from B60 + 21 from B61) → **tier-1 `gate: PASS (passed=16 failed=0)`** → `python -m pytest tests/scripts/` 12/12.
  - **Still NOT RUN, honestly:** an actual redrawn pixel on screen needs a configured image-generation provider (设置 → 图片生成); this machine has none. No static image or mock is substituted. §57's "Figure Redraw = PASS" therefore remains open.

- **Batch 62 (the packaged re-verify stops being an orphan):** `scripts/agentc-pack-final-verify.cjs` was the repo's only real packaged-exe CDP journey, yet it appeared in no tier manifest and no npm script — meaning §56 depended on someone remembering to run it by hand. Registered as **`packaged-cdp-smoke` in tier 4** (`requires: ["release/win-unpacked"]`, `evidence: ["logs/pack-final-verify.json", "docs/screenshots/phase2-pack-final"]`, `required: true`, 20-min timeout), so `verify:reliability:release` now covers it and a missing packed build is a visible NOT RUN rather than a silent skip. Also fixed a default that made re-verification untrustworthy: the script resolved `METIS_PACKED_DIR || 'release/final'` while electron-builder's configured output is `release` (so `release/win-unpacked`) — every run had to pass the env var explicitly or it would silently verify a stale directory. Verified through the authoritative runner with no env override: `node scripts/reliability-gate.mjs --tier=4 --only=packaged-cdp-smoke` → **`gate: PASS (passed=1 failed=0)` in 25.7 s**, report `pass=true` with **35/35 checks**, 6 captures, `metisMethodCount=590` (the bridge-set-vs-preload-snapshot check is one of them, so B61's new `uaRedrawFigure` is asserted present in the exe), a genuine 40-chunk streamed reassembly, graceful WM_CLOSE then same-profile relaunch with the accent and the `PersistenceStore initialized` counter both proving persistence.

- **Batch 63 (§26's missing lifecycle half is now real):** B61's audit found `start → expose → release` absent — task execution called `provider.complete(messages)` with no tool loop while `MCPManager.loadAndConnectAll()` connected every enabled server eagerly at boot, so "MCP 按当前任务动态加载" was selection-only. Implemented the five-step clause where it belongs:
  - `engine/mcp/MCPManager.ts`: `startForTask(ids)` → `{started, borrowed, unavailable}` (connects only **enabled-and-not-yet-connected** rows; a disabled or unknown id is *reported*, never started, because enabling is the user's consented act in Settings; servers already online are `borrowed` and are deliberately **not** released, so a task can never cut the chat path's connection), plus `releaseFromTask(started)`, `toolsForServers(ids)`, `toolNamesForServers(ids)` (same `mcp_<server>_<tool>` naming rule `connectServer` registers under, so the allowlist matches the registry).
  - `createDomains.executeNextTaskForProject.runModel`: resolve → `startForTask(loadedMcpIds)` → `agentLoop.run({messages, allowedTools: toolNamesForServers(...), maxTurns: 8, projectId, …})` (the same loop ChatTurnService/ScenarioWorkflowService use, so HITL approval semantics are not bypassed) → final text; `finally` releases. With no tools to expose it falls back to the previous single-shot `provider.complete`, which is why B58/B59's verified execution journey was unaffected. Start/borrow/unavailable counts and the number of tool calls land in `provenance.note`.
  - Verification: `tests/electron/McpPerTaskLifecycle.test.ts` **3/3** against the repo's **real stdio MCP fixture** (`tests/fixtures/mcp/leak-probe-server.mjs`) — disabled→unavailable+not connected; enabled-unconnected→started, `toolsForServers=[probe]`, `toolNamesForServers=['mcp_task-srv_probe']`; release→disconnected, tools gone, startable again; already-connected→borrowed and release-of-started leaves it up. Plus `ChildProcessLeakGate` (20 real servers, every child reaped) and the existing `taskExecutionService` / `TaskPlanExecuteIpc` regressions (10/10), `scripts/child-process-leak-gate.cjs --cycles=20 --focus=terminal,genoffice` → **gate: PASSED, ports free**, and **§55 rounds A (09:29:17Z) + B (09:31:51Z): 7/7 steps exit 0 each, sweep 41/41, capture 49/0 warnings** on this tree.
  - Honest residuals: a live task where the model *spontaneously* calls an MCP tool still needs a user-configured, enabled MCP server + provider → NOT RUN (the deterministic tests cover lifecycle + exposure surface, not model-initiated invocation). `loadAndConnectAll()` still eager-connects enabled servers at boot, so "默认不启动所有 MCP" holds in the weak sense only; making per-server autostart default-off changes existing users' behaviour and was not bundled in here.

- **Batch 64 (the objective's last clause — 再加工 + 持续沉淀 — is now verified live, and across a restart):** `ui-feature-sweep` grew a step 4d and two relaunch assertions, so the converged right panel is exercised as a work surface rather than a viewer:
  - 再加工: select the row of the artifact the model just produced, type into the panel's own editor, press 另存为新版本 → version chips go `1 → ["v1","v2"]` and §18's `artifact-version-diff` renders. Nothing about it needs a model, and it is the first time the live app is asserted to **append** a version from the UI (the old coverage was jsdom-only, and B58 had already shown that panel-level live behavior can hide a real defect).
  - Restart persistence (§56's `Restart Persistence` line, previously covered only for chat history and the accent setting): hard-quit, relaunch on the same profile, then through the real bridges — `taskPlanGet` → `["T001:COMPLETE","T002:READY","T003:TODO","T004:TODO"]` (§30's derived states survive), `uaList` → the executed artifact still has `[1,2]` versions and the appended marker is in the current content. Evidence is recorded in the relaunch report's `evidence.durability`, and A/B rounds produced identical values.
  - Sweep grew 41 → **42** assertions and the relaunch phase 4 → **6**; `phase2-full-sweep`'s descriptive baseline was updated to `42/42 + relaunch 6/6` (that column is documentation only — status still comes from exit code + `overall`). Two clean rounds afterwards: A 10:05:17Z, B 10:08:08Z, **7/7 steps exit 0 each**, matrix 106/106, captures 49/0 warnings.
  - Two harness bugs found and fixed while doing this (product untouched): the inserted text used `'\n\n…'` inside the outer template literal, so the page received real newlines inside a string literal and threw a SyntaxError (escaped to `\\n\\n`); and the relaunch phase assumed `listProjects()` returns an array — it does not, so the project lookup now normalizes both shapes. `eslint` clean on both scripts; Node ABI restored afterwards.

- **Batch 65 (the delivery chain is live-verified, and it caught a 9th P1):** `ui-feature-sweep` gained step 4e — click 「登记投稿」on the manuscript row, type the journal, 「确认登记」, open 投稿进度, then 「预检」/「生成并导出材料包」/「校验材料包」/「冻结材料包」, all through the panel's own buttons. Measured on real Electron: `pf = 预检通过（13 提示）`, `exported = 材料包已导出 1 项：<profile>/submissions/<caseId>/round-1` with the harness verifying that directory from the main process (`fs.existsSync` + non-empty), `validated = 校验：1 有效 · 0 无效 · 0 待确认 · 0 待定`, `frozen = 材料包已冻结` plus the 「已冻结」badge. The freeze assertion is conditional on the preflight verdict (pass ⇒ must really freeze and show the badge; fail ⇒ must name the blockers), so it cannot pass vacuously.
  - **Defect #14 (P1), found by that run rather than by reading code:** `registerSubmission` reported success but never refreshed `cases`, so the panel simultaneously said "已为「…」登记投稿案件" and 投稿进度 still read "尚无投稿案件（0）" — the case only appeared after the user navigated away and back. Exactly the B58 family (state changed behind a component that only loads on mount). First attempt scored 44/48 with 4 delivery assertions red for this reason; fixed with `await reload()` after a successful registration and pinned by a jsdom regression that counts the `listSubmissionCases` calls (mount + refresh) and then asserts the case row and the 「投稿进度（1）」counter — `ArtifactsPanel.test.tsx` 26/26.
  - Also this batch: the sweep's manuscript is seeded into the UI-created project, so the harness now resolves that project's id from `listProjects()` (normalized for object-or-array shape) and remounts the panel by navigating 扩展 → 项目, the same way a user would, rather than giving the component a test-only force-refresh hook. `phase2-full-sweep`'s descriptive baseline updated to `48/48 + relaunch 6/6`.
  - **Re-measured after the fix:** §55 rounds **A 10:24:30Z and B 10:27:09Z, 7/7 steps exit 0 each**, sweep 48/48, relaunch 6/6 with identical `durability` evidence (`versions=[1,2]`, marker present, plan states `T001:COMPLETE/T002:READY/T003:TODO/T004:TODO`), matrix 106/106, captures 49/0 warnings; **full `vitest run` 6219 passed / 0 failed / 9 skipped (6228), 582 files**; **tier-1 `gate: PASS (passed=16 failed=0)`** (`logs/b65-vitest-full.log`, `logs/b65-tier1.log`).
  - **Packaging hiccup recorded honestly, not smoothed:** the first `npm run pack` of this batch failed with `EPERM` renaming `win-unpacked.tmp` then `UNKNOWN: open '…Metis Research Workbench.exe'` while app-builder edited resources — a transient Windows file lock on the freshly written exe (the verifier itself does close both sessions; `taskkill` + wait + `killTree`, and no `Metis Research Workbench.exe` process from `release/win-unpacked` was alive afterwards). No product or harness change was made for it; the pack was re-run and the tier-4 `packaged-cdp-smoke` check re-executed, and only its result backs the packaged claim.

- **Batch 66 (the right panel could not actually produce most of its own 阶段产出 — audited mechanically, then wired):** the objective's last clause says stage outputs and deliverables keep accumulating *in the right panel*. Auditing that claim by reading code is how B65 nearly shipped a false one, so this batch measured it: `uaSaveLiteratureLibrary / uaSaveRunAll / uaSaveTopicCandidate / uaSaveResearchMaterials` had IPC handlers, engine tests and preload methods but **zero renderer call sites** — the panel could only *display* those stages, never produce them (`uaCreate` was likewise uncalled). A screening script (`scripts/audit/bridge-usage-audit.mjs`, cross-checks all 436 bridge method names against `src/`) generalizes the finding: **106 methods are never named in the renderer**. That number is a screen, not a verdict — it includes harness-only APIs (`acceptance*`), event subscriptions and dynamically dispatched names — but it is what turned "did we wire everything?" from a memory test into a measurement.
  - Wired three of the four into `src/research/ArtifactsPanel.tsx` as real forms, each behind a collapsed 阶段产出 action so nothing changes for a user who ignores them: **④ 研究材料** (row editor: 名称/类别/位置/格式/已就绪 → `uaSaveResearchMaterials`), **① 文献准备** (reads the *real* paper library through `window.metis.listPapers()`, filters to entries tagged with this project, renders them via `uaSaveLiteratureLibrary` with a GB/T 7714 / APA choice, and **names its own scope** in the confirmation line — "本项目的 N 篇" vs "文献库（尚未按项目标注）的 N 篇" — rather than letting a widened scope pass as a project-scoped one), **② 选题确认** (题名/研究问题/理由/意义/可行性/关键词 → `uaSaveTopicCandidate`, and the row it creates feeds the already-wired「确认为选题」, so §7's candidate→confirm step is now a user path end to end; re-saving the same title appends a version instead of duplicating).
  - `uaSaveRunAll` (⑤ 可复现运行) is **deliberately left unwired**: its input is `ReproCodeStep[]` (ordered script paths), and nothing in the product produces that list — the only honest call site would be a code-editor surface (§14 keeps rich editing in standalone GenOffice). Filing it from a form would mean inventing the steps, i.e. the §25 "no hollow" rule inverted. Same for `uaCreate`: a generic "把这段存为产出" affordance belongs in the Conversation's preview pane, which is the next real gap on this clause, not a panel button.
  - **Defect #15 (P1) found by the live step, not by reading:** the sweep seeded a library paper with `savePaper({projectId})` and the panel reported `mine: 0`. `paper:save` decodes a contract that **advertises `projectId`** and then writes `existing?.projectId` — and `PersistenceStore.savePaper` keeps whatever is already stored (`project_id = COALESCE(papers.project_id, excluded.project_id)`), so a caller that re-assigns a paper gets a success code plus an unchanged row: the association lives in renderer memory until restart, then disappears. The library page hides this by also calling `paper:linkToProject` (the `paper_project_links` table is what `getPapers()` derives `projectIds` from), which is why no unit test had caught it. Fixed in `registerPaperIpc.ts`: the caller's `projectId` now wins, and a *changed* one additionally goes through `setPaperProjectId` (savePaper's COALESCE cannot move it). `tests/electron/PaperProjectLinkRoundTrip.test.ts` **4/4** drives the real handler + real SQLite and pins all three cases, including the two that must NOT change: no `projectId` sent ⇒ stored value kept, and `projectIds` stays link-derived (a saved plural list is not a link).
  - Verification: `ArtifactsPanel.test.tsx` **33/33** (+5: literature payload+style exact, empty-library refusal, untagged-scope disclosure, candidate payload with derived id and split keywords, missing-field refusal), the new round-trip test 4/4, `typecheck`(4) exit 0, `eslint` clean on the touched files. `ui-feature-sweep` grew steps 4g/4h and two relaunch assertions: **52/52 main + relaunch 9/9 PASS** (`evidence.topicStage`: `row true, label ② 选题确认, confirmed true, hadConfirmedBefore false`; `evidence.literatureStage`: `label ① 文献准备`, `note 已把本项目的 1 篇文献…`, `literatureSeed.mine 1`), and `evidence.durability` now also carries `topic:{candidateId 'topic:真机扫掠候选选题', candidateStage 'topic', confirmedId 'confirmed_topic', markerSeen true}` and `literature:{id 'literature_library', stage 'literature', markerSeen true}` across a hard quit + same-profile relaunch. `phase2-full-sweep`'s descriptive baseline moved to `52/52 + relaunch 9/9`.
  - No IPC channel or preload method was added, so the three contract baselines (ipc-inventory, api snapshot, `IpcRegistryLifecycle` ua count) stay untouched — confirmed by the probe step reporting `555/557 · missing 0 · unknown 0 · strict 0`.
  - **§55 re-pair on this tree:** rounds **A 11:13:43Z and B 11:16:32Z, 7/7 steps exit 0 each**, sweep 52/52 + relaunch 9/9, and `topicStage` / `literatureStage` / `materialsStage` evidence byte-identical between rounds.
  - **Harness defect found while doing this (#17, report-side only):** `phase2-full-sweep.mjs::collectAutoNotes` still compared against wave9 numbers (`sweep 30`, `matrix 196`, `capture 67`, `layout 20` shots), so *every* report since B56 carried an unconditional "与近期基线存在数字偏差" note — an alarm that can no longer be trusted to mean anything. The tripwires now hold the converged口径 (52 / 106 / 49 / 12), and the probe one checks what it was always meant to check (`missing = unknown = methodMissing = strict = 0`) instead of an exact channel count that legitimately moves with the product. Regenerating both reports yields "无。全部步骤一次通过、无重试、无与近期基线的数字偏差". Status still comes only from exit code + `overall` — this changes the note, not the verdict.

- **Batch 67 (§5's spine was a filing cabinet with one drawer):** `executeNextTaskPersisted`'s host saved every task output with a hardcoded `stage: 'execution'`, so whatever strategy a project chose, its accumulated output could only ever pile up in **⑤ 分析执行** — ③ 研究设计 had *no producer anywhere* (`grep "stage: 'design'"` matched only `StageDetector`/`CapabilityRouter`, which are a different, pre-convergence stage model), and ⑥ 阶段成果 likewise none. "阶段产出按 §5 生命周期持续沉淀" was true of storage, not of the lifecycle.
  - New `engine/research/strategyStageMap.ts` is now the single table: strategy action → plan section label → spine stage (17 actions → ①②③④⑤⑥). `taskPlanGenerator` lost its private `ACTION_SECTION` copy and reads sections from it, so a label cannot exist without a stage; `taskExecutionService` passes the stage in `SaveArtifactInput`; `createDomains` honors it, with ⑤ kept as the fallback for a hand-authored section the table does not know (an unknown label must not disappear from the panel, and must not silently relocate existing output either).
  - Tests: `engine/research/strategyStageMap.test.ts` **6/6** (every mapped stage ∈ `ARTIFACT_STAGES`; the mapped set spans exactly ①②③④⑤⑥ — so collapsing the table back to ⑤ fails; section labels unique so the reverse lookup is unambiguous; unknown → `undefined`), `tests/engine/taskExecutionService.test.ts` extended (real preset's first task now files as `stage:'topic'`; a two-task plan with sections `研究设计` + `附录整理` files `design` then the `execution` fallback while both complete).
  - **Live, and falsifiable the other way:** `ui-feature-sweep`'s spine assertion no longer hardcodes ⑤ — it reads the executed task's own section title out of the plan panel and looks the expected group up in a *separately hardcoded table inside the harness*, so drift in either direction (product returns to ⑤, or the product table changes) goes red. Measured: `evidence.taskSpine = [{taskId:"T001", section:"相关研究", group:"stage-group-literature", title:"① 文献准备"}]`, assertion `true`. `gate:metis2-product` 12/12 and `gate:architecture` (604 files, 0 cycles) after the refactor; typecheck(4) 0; eslint clean.
  - What B67 does NOT claim: ③ and ⑥ are reachable-by-mapping and covered by deterministic tests; the sweep executes only the plan's first task, so a live design/writing-stage filing has not been clicked. Recorded rather than rounded up.
- **Batch 68 (⑦ 交付 gets a real record, and `ua:create` finally has a caller):** the delivery chain proved a package could be assembled, exported, validated and frozen (B65), yet nothing on the spine said so — stage `delivery` had no producer and `uaCreate` was the fourth uncalled artifact-writing method. `ArtifactsPanel` now files the just-exported package as `delivery:<caseId>` with `stage:'delivery'`, built only from facts that export returned (package id, export directory, per-file list, and the preflight verdict if it was run in this session); re-exporting the same case goes through `uaNewVersion` so the earlier record is kept as a version rather than overwritten (§20.1 — `store.create` on an existing id would have replaced the row outright, which is why the panel checks first).
  - Tests: the §29 export case now asserts the filed payload (id/stage/title/package id/file path/provenance source) and a new case pins the append-not-replace rule (`uaNewVersion` called, `uaCreate` not) → `ArtifactsPanel.test.tsx` **34/34**. Live: `artifact-row-delivery:sub-case-08809782-…` present under 「⑦ 交付」 (`evidence.delivery.deliveryRow true / deliveryTitle ⑦ 交付`), sweep **53/53** + relaunch **9/9**.
  - **§55 pair + §51 re-run after B70:** A 12:45:19Z / B 12:47:52Z, 7/7 steps each, sweep **58/58** + relaunch **9/9**, with `replanningIsolation` identical byte-for-byte across rounds alongside the other five evidence blocks; `rebuild:node` → **full `vitest run` 6249 passed / 0 failed / 9 skipped (6258), 586 files** → **tier-1 `gate: PASS (passed=16 failed=0)`** (`logs/b70-probe/vitest-full.log`, `logs/b70-probe/tier1.log`).
  - **§55 pair re-run after B69** (harness-only change): A 12:05:00Z / B 12:07:35Z, 7/7 steps each, sweep **55/55**, and the five spine/accumulation evidence blocks are byte-identical between rounds.
  - **§51/§55/§56 re-measured after B67+B68, in dependency order:** typecheck(4) 0 → `eslint` clean on every touched file → `gate:metis2-product` 12/12 → `gate:architecture` 604 files / 0 cycles (the new `strategyStageMap` module introduces no edge back into `taskPlanGenerator`) → `build:electron` → **§55 rounds A 11:42:19Z / B 11:44:59Z, 7/7 steps exit 0 each**, sweep **53/53** + relaunch **9/9** (matrix 106/106, captures 49 / 0 warnings; `taskSpine`/`topicStage`/`literatureStage`/`materialsStage` byte-identical across rounds, `delivery` identical except the uuid/profile path minted per run) → `rebuild:node` → **full `vitest run` 6238 passed / 0 failed / 9 skipped (6247), 584 files** (`logs/b68-probe/vitest-full.log`; 6230 + 6 map + 1 execution-stage + 1 append-version) → **tier-1 `gate: PASS (passed=16 failed=0)`** → `build:electron` → `npm run pack` exit 0 → **tier-4 `packaged-cdp-smoke` PASS (35/35 checks, 590 bridge methods, 6 captures)** at 11:59:30Z, so §56 is re-proven on this tree rather than inherited.

- **Batch 69 (持续沉淀 = more than one row, and each in its own drawer — harness only):** the spine assertions proved *where one* output lands; "持续" says output should keep arriving. `ui-feature-sweep` now clicks run-next a second time in the same journey and reads every `artifact-row-task:*` row's group plus its own task's section heading, expecting each from the harness's own copy of the section→stage table. Measured on real Electron: `before 1 → after 2`, badges `["完成","完成","可执行","待办"]` (§30 unlocking逐级), and the two rows sit in **two different stages** — `T001 相关研究 → ① 文献准备`, `T002 分析 → ⑤ 分析执行`. Before B67 both would have piled into ⑤, so this is the lifecycle claim made visible rather than asserted once. Sweep 53 → **55** assertions, `phase2-full-sweep` baseline + tripwire moved to `55/55`.
  - **The ABI trap bit this batch in the informative direction:** after `rebuild:node` (to leave the repo vitest-ready post-pack) I launched the sweep against the still-Electron-ABI `dist-electron` build, and step 2 died with `setup_runtime_rebuild_failed` / "first-run provider save activates runtime" — not an `NODE_MODULE_VERSION` message. That is the app-side face of the same single-ABI constraint (SQLite cannot open ⇒ first-run setup cannot persist), so `npm run rebuild:electron` is a prerequisite for any Electron-driven harness, and re-running after it turned the whole sweep green with no product change.

- **Batch 70 (two more silent-fallback defects, both found by trying to *prove* B67 rather than by reading it):** to test stage filing end to end, the acceptance sweep needed to re-plan the same project from a *different* Research Strategy — and that single attempt exposed two product defects plus one missing affordance.
  - **Defect #18 (P1) — output identity keyed by task slot.** `executeNextTaskForProject` saved as `task:${taskId}`. Task slots are reused by every regenerated plan, so the new strategy's first task appended a version to the *previous* strategy's artifact: one row, the old title, the old §5 stage, two unrelated phases interleaved — which silently cancels B67's stage filing for any project that was ever re-planned. Fixed with `taskArtifactId({id,title})` (`task:<slot>:<slug>`): re-running the same task still appends a version (§20.1), different phases become different artifacts. `tests/engine/TaskArtifactIdentity.test.ts` **5/5**, one case deliberately *reproducing* the old scheme on a real store (it asserts the design-phase output ends up under `stage: 'literature'` with the other task's title, so the reason for the rule cannot be "simplified" away later).
  - **Defect #19 (P1) — a preset picked in the picker was ignored.** `ResearchStrategyStore.getStrategy()` read only persisted rows, while `listStrategies()` merges presets + legacy scenario projections — and the panel's `<select>` is built from that merged list. So selecting any built-in made `taskplan:generate` fall back to the **default** strategy and still return success: "Research Strategy 负责研究方法" did not hold for exactly the strategies shipped with the product. B47 could not catch it because it called `generateTaskPlanFromStrategy` directly, bypassing resolution. Fixed: a miss on the persisted row now resolves through `listStrategies()` (user rows with the same id still win). `tests/engine/StrategyResolution.test.ts` **5/5**, including the general invariant *"every id that is listed must be resolvable"* rather than a per-source patch.
  - **#20 (P2, affordance) — the strategy picker vanished once a plan existed** (`{!tasks.length && …}`), so re-planning was unreachable in the product while the panel's own empty-state text told the user to do it. The header now always offers the picker; with a plan present the button reads 「按所选策略重新生成计划」and a hint states that only the task list is replaced and existing 阶段产出/version history are kept — the promise B70's identity rule actually delivers. `TaskProgressPanel.test.tsx` **8/8**.
  - **Live proof (sweep step 4i):** switch the picker to `preset_theoretical_interpretation` → 重新生成 → 执行下一任务, reading artifacts through `uaList`. Measured (`logs/b70-probe/sweep4.json`, **58/58** + relaunch **9/9**): a **new** row `task:T001:问题化` appears with `stage: 'topic'`, DOM group `stage-group-topic` and store group in agreement; the pre-existing `task:T001:文献综述` keeps `stage: 'literature'` and its 2 versions untouched; `untouched: true`, `fresh: ["task:T001:问题化"]`. Two harness lessons recorded the honest way: the first run scored 53/57 because the row→task lookup had to take the slot segment of the new id, and the "every row must match its stage" assertion was corrected to apply only to rows this plan produced — asking a *previous* plan's artifact to follow the *new* plan's section is a wrong expectation, not a product bug (old-row integrity is covered separately by `untouched`).

- **Batch 71 (the packaged acceptance could go green without running):** re-verifying §56 on the B70 tree produced `pack-final verify: PASS (31/31)` — and `31` was the giveaway. The run had thrown `Error: Execution context was destroyed.` in the relaunch stage, so the last four checks (same-profile relaunch ready, accent still gold, PersistenceStore init count grew, relaunched process alive) were never written, yet `report.pass = checks.every(ok)` happily greened a truncated journey. Two fixes:
  - `pass` now requires **zero failures + no recorded error + all 11 mandatory check names present**, and prints `missingMandatory=n` when short. The same truncated run immediately became `FAIL (31/31) missingMandatory=4` — the guard catches the real thing rather than a hypothetical.
  - Root cause, not workaround: the first CDP `eval`s after relaunch race the page's context being recreated. `Cdp.eval` now retries only that class of transient error (≤10 × 700 ms) and still requires the expected value; assertions are unchanged in strength. Two consecutive full runs then passed **35/35** with `restart = {ready2: true, accent2: 'gold', accentVar2: '#d97706', storeInitBefore: 1, storeInitAfter: 2}` (13:12:06Z / 13:12:29Z, 22 s each, 40-chunk streamed reassembly, 590 bridge methods).


- **Batch 72 (the spine claim is now measured, not inferred — harness only):** B70's re-planning step proved one new row; the claim "every §5 stage has an output path" still leaned on ③ 研究设计 / ⑥ 阶段成果 being map-only. Step 4i now **drains** the regenerated 13-phase plan (click 执行下一任务 until the affordance is naturally unavailable, ≤16 rounds, re-reading the store each round). Measured (`logs/b72-probe/sweep.json`, **59/59** + relaunch **9/9**): `executions: 13`, `fresh: 13`, `stalled: 'run-next-unavailable'` — the plan was exhausted rather than the loop cut short — `stagesCovered = [stage-group-design, stage-group-execution, stage-group-literature, stage-group-outcome, stage-group-topic]`, no row whose DOM group or stored `stage` disagrees with the harness's own section→stage table, and `untouched: true` for every artifact produced before the re-plan (e.g. `T008 批判与辩护 → ③ 研究设计`, `T013 论文写作 → ⑥ 阶段成果`). With 4f's ④ 研究材料 and B68's ⑦ 交付 registration, **all seven spine stages now have live evidence**; report §18 row 11 is closed. Sweep 58 → **59** assertions.
- **Batch 73 (tier 2 could never pass, and one report edit that had to be disclosed):** re-running the desktop tier on the B72 tree to refresh §14's inherited claims produced `gate: FAIL (passed=4 failed=1)` — `vitest-mcp-leak`, an in-process vitest gate needing the Node ABI, sits in tier 2 whose other four gates spawn the built app and therefore need the Electron ABI, and the single native module can only be one. Fix: `vitest-mcp-leak` moved to tier 1 (it is deterministic, ~12 s, PR-appropriate). Measured after the move on this tree: **tier-1 17/17** on Node ABI (`logs/b73-probe/tier1.log`), **tier-2 4/4** on Electron ABI (`logs/b73-probe/tier2b.log`: layout 9.4 s, shutdown-relaunch 7.1 s, child-process-leak 65.2 s, crash-relaunch 15.3 s) — so §14's desktop rows are now re-measured rather than inherited from B63.
- Same batch also produced **#22** (文献阅读元数据重启即失：契约接受 `starred/archived/priority/deadline/readingProgress`，`papers` 无列且视图 omit 这些字段) — located, pinned by an explicit current-behaviour test in `PaperProjectLinkRoundTrip.test.ts`, and left OPEN with the reason written down (needs a column migration; not one of the six clauses), plus **the report itself was damaged and repaired**: a scripted edit to `docs/metis2-final-report.md` dropped its `s[j:]` tail and truncated the file at §15. §16/§17/§18/§18.1/结论 were rewritten from this session's verified measurements; the rewrite is announced inside §16's preamble rather than passed off as the original prose, and the truncated state is preserved at `logs/report-truncated-backup.md`. 23 defect rows validated well-formed afterwards.
- **Batch 74 (defect #22 closed: the paper library's reading metadata was written into columns that did not exist):** `LibraryPaperSaveRequestSchema` accepts `starred / archived / priority / deadline / readingProgress / readingTimeSeconds / readAt`, `PersistenceStore.savePaper` had no column for any of them, and `LibraryPaperViewSchema` omitted all seven — so `paper:save` answered `saved` while the values died with the process. Not hypothetical: the 文献库 ★ is a live workbench control (`ProjectsPage` mode `materials` mounts `LibraryPage`), and `GlobalSearch` (`is:starred`, `priority:`, `deadline:`) plus the dashboard reading stats all read fields nothing could have stored. Fixed along the whole chain rather than one layer:
  - **Migration 119** adds seven **nullable** columns. Nullable on purpose: `savePaper` now writes `COALESCE(excluded.col, papers.col)`, because most callers (browser collector, watch service, PDF import, dedupe/merge, agent tools) build partial records by hand and an absent key must not erase what the user set — the same trap `project_id` already documents. An explicit `false`/`0` still wins, so un-starring and progress resets persist; "clear a text field back to unset" is deliberately not expressible through this writer (nothing in the product does it, and the comment says so instead of inventing a sentinel).
  - `getPapers()` returns the seven (`0/1` → boolean, `NULL` → `undefined` so a `0` progress survives), `paper:save` forwards them, and the view's `omit` list shrinks to the two keys the safe projection really does keep off the renderer (`url`, `referenceIds`) — the comment in `presentPaper` about `referenceIds` staying omitted is still load-bearing: one un-declared key makes `decodeLibraryPaperList` return `[]` and empties the whole 资料库.
  - One field had readers but no writer at all: nothing ever set `readAt`, so 本周阅读 / 连续阅读 / 近 N 天已读 could never be non-zero. `store.updatePaper` now stamps it on the transition to `read` (the same funnel the ★ uses), which makes those surfaces real instead of decorative.
  - **Verification:** `PaperProjectLinkRoundTrip.test.ts` 8/8 — the pinning case flipped to a round trip, plus a case that **closes and reopens the same database file**, a partial re-save that must not wipe stored metadata, and an explicit un-star. `LibraryPage.test.tsx` 15/15, with the star case strengthened from "the in-memory store flipped" (exactly what hid the defect) to asserting the IPC payload carries `starred: true` and the list badge renders. Real Electron: `ui-feature-sweep` 59 → **60** assertions (4h now reads `starredBack: true / priorityBack: 'high'` back out of `paper:list`), relaunch child 9 → **11** — after a hard quit + same-profile restart `paper {starred: true, priority: 'high'}` survives, and the 资料 tab shows the ★ with `badgeBefore true → badgeAfter false` from a real click. typecheck(4) 0, eslint clean, `gate:architecture` 604 files/0 cycles, `gate:metis2-product` 12/12, the old-DB fixture/drift matrix green (legacy rows yield `NULL` → `undefined`, so the strict view still decodes), **tier-2 4/4** on Electron ABI (`logs/b74-probe/tier2.log`), **§55 pair re-run on this tree** (A 14:42:47Z / B 14:45:40Z, 7/7 steps each, sweep 60/60 + relaunch 11/11, all seven compared evidence blocks plus the whole `durability` byte-identical), `rebuild:node` → **full `vitest run` 6254 passed / 0 failed / 9 skipped (6263), 586 files** (`logs/b74-probe/vitest-full.log`; 6249 + 3 paper + 2 library) → **tier-1 17/17** (`logs/b74-probe/tier1.log`) → `rebuild:electron` → `npm run pack` exit 0 → **packaged CDP end-to-end smoke 35/35** (`logs/b74-probe/tier4-smoke.log`) → **pytest 12/12**.
  - **A re-measurement caught a flake, and it is recorded rather than re-rolled:** the full-suite run taken to refresh §15's totals came back **1 failed** — `FundingTemplatePanel.test.tsx:329` asserted `document.activeElement` synchronously after `findByRole('alert')` while the panel focuses the alert from a passive effect; under full-suite load the node appears before the flush. It passes in isolation and across `tests/frontend` (115 files / 1335 tests), i.e. a timing assumption rather than a product bug, and its sibling case three lines above already used `waitFor` for the same assertion — this case now does too. The same run also failed `StartupHealthService.test.ts`, which hardcoded `migrations applied to: 118` — migration 119 legitimately advanced it, so the expected version now derives from `UNIFIED_MIGRATIONS` instead of a magic number that would fire on every future migration. The general lesson: a green full-suite number measured on an earlier tree is not a number for this one.
- **Batch 75 (#22 was not a one-off — the same silence existed on the notes table, and the rest of the record domains were checked):** B74's fix was written up as "contract accepts → writer drops → view cannot show", so the next question is where else that shape lives. Applied mechanically to the SQLite record domains, one more live instance surfaced: **`LibraryNoteSchema` declares `starred`** (`engine/runtime/LibraryRuntimeContract.ts:112`), `src/pages/NotesPage.tsx` has two real ★ buttons (row + detail) driving `store.toggleNoteStar → updateNote → metis.saveNote`, `GlobalSearch` filters `is:starred` over notes and the dashboard counts them — while `note:save` never forwarded the key, `notes` had no column, and `getNotes` could not return it. Same success answer, same restart amnesia. Fixed along the same chain: **migration 120** (`notes.starred`, nullable), `saveNote` writes it with the same `COALESCE(excluded, notes)` carry-forward rule (an absent key keeps what is stored, so partial bulk jobs cannot erase it; an explicit `false` still un-stars), `getNotes` returns `NULL → undefined`, and `note:save` forwards it. New `tests/electron/NoteStarRoundTrip.test.ts` **5/5** through the real handler + real writer: round trip, **close and reopen the same database file**, explicit un-star, partial re-save not wiping, and a pre-migration row leaving `starred` absent rather than breaking the strict list decode (the #7 failure mode).
  - The rest of the audit is recorded as register row **#25** so nobody re-derives it: collections are exact 5/5/5 parity; experiments' ★ rides `saveExperimentMetadata`/`getExperimentMetadata`, which do select and write the `starred` column (the legacy `saveExperiment` has no product caller left); `mcp_servers` is 7/7/7 parity; `workflow_runs` / `eval_runs` writers cover every column they declare, and `WorkflowRunItem`'s extra `workflowName` / `progress` have neither a construction site nor a consumer in the repo (dead declaration — P2 dead code, not data loss); the JSON-backed domains were covered by B53 / #18 / #19.
  - **Verification on this tree:** typecheck(4) 0, eslint clean on the touched files, the 4-file persistence run (note round trip + `PersistenceStore` + library paper round trip + old-DB fixture/drift matrix) **150/150** — the drift matrix matters because migration 120 has to leave a fresh database and an upgraded one identical. `build:electron` → `ui-feature-sweep` **61/61** with a new main-phase assertion (`noteSeed.starredBack true`) and a 12th relaunch assertion (`durability.note {id 'sweep-note-1', starred true}` after a hard quit + same-profile restart), **§55 pair A/B each 7/7 steps exit 0 with the four compared evidence blocks identical**, **tier-2 4/4**, then `rebuild:node` → full `vitest run` **6259 passed / 0 failed / 9 skipped (6268), 587 files** (`logs/b75-probe/vitest-full.log`) → **tier-1 17/17** (`logs/b75-probe/tier1.log`) → `rebuild:electron` → `npm run pack` exit 0 → packaged CDP smoke **35/35** (`logs/b75-probe/tier4-smoke.log`) → **pytest 12/12**.
- **Batch 76 (turning two found defects into a gate that makes a third one a build failure):** #22 and #24 were the same accident on two tables — a strict save contract declares a field, the writer has no column, the reader cannot return it, and the save answers `saved`. Both were found by hand. `tests/engine/RecordFieldParity.test.ts` now checks that per-claim mechanically for the four SQLite record domains (papers / notes / collections / experiments-metadata): every key in `schema.shape` must either round-trip through the real `PersistenceStore` writer + reader or appear in `exclusions` with a written reason, and the reverse assertions stop the escape hatch from rotting (an exclusion for a key the contract no longer declares fails; a declared key in neither `sample` nor `exclusions` fails). Three exclusions exist, each with its reason: papers' `url` (no column, collection provenance lives on `pdfUrl`), `pdfCapability` (issued per window by `FileCapabilityRegistry`), `projectIds` (derived from `paper_project_links`; `paper:linkToProject` is the writer). Experiments are sampled with `starred: true` because `getExperimentMetadata` emits the key only when 1 — a representation difference written into the test rather than left to be rediscovered.
  - **RED-proved, not assumed:** with `getNotes`'s `starred` read patched to `undefined`, the new gate and `NoteStarRoundTrip` fail together (`field "starred" did not survive the writer: expected undefined to deeply equal true`); reverting restores 8/8. Registered as tier-1 check `vitest-record-field-parity` (it also runs the two real-IPC round-trip files), so **tier-1 is now 18 checks**. No product code changed in this batch, which is why §55 / tier-2 / packaged legs were not re-run: the last full desktop re-measurement remains B75's (sweep 61/61 + relaunch 12/12, pair A/B 7/7, tier-2 4/4, pack + CDP 35/35, pytest 12/12).
  - **Verification:** typecheck(4) exit 0, eslint clean on the new/changed files, full `vitest run` **6267 passed / 0 failed / 9 skipped (6276), 588 files** (`logs/b76-probe/vitest-full.log`; 6259 + 8), **tier-1 `gate: PASS (passed=18 failed=0)`** (`logs/b76-probe/tier1.log`).
- **Batch 77 (same gate, now aimed at the JSON-record stores — where the loss mechanism is different):** B76's gate only covered SQLite columns. The right panel's own records live in the shared `memory` table as serialized JSON, so the failure mode there is not a missing `ALTER TABLE` but a serializer/parser pair that rebuilds an object and forgets a key. `tests/engine/RecordFieldParity.test.ts` gained a second block over `UnifiedArtifactStore`: `create` → re-read must **deep-equal** the object the store returned, then the same for the appended version (`newVersion`, with a widened `provenance.mcpIds`), the `setStatus('final')` promotion that turns a 阶段产出 into a 交付成果, and the `list()` view. `provenance.skillIds` / `provenance.mcpIds` are asserted by name because those arrays are clause 5's only durable audit trail ("Skill/MCP 按当前任务动态加载" — if they vanish on reload, the claim is unverifiable rather than false). `parseArtifact` turned out to return the whole parsed object (no field-by-field rebuild), so the read path is structurally safe today; the block is still RED-proved rather than assumed: deleting `taskId` inside `parseArtifact` made the create case fail with a `- "taskId": "T001"` diff, and reverting it (confirmed by an empty `git diff` on that file) restored **12/12**. **Verification:** typecheck(4) 0, eslint clean, full `vitest run` **6271 passed / 0 failed / 9 skipped (6280), 588 files** (`logs/b76-probe/vitest-full-b77.log`; 6267 + 4), tier-1 **18/18** (`logs/b76-probe/tier1-b77.log`). No product code changed, so the desktop legs still date from B75.
**SCENARIO-ENTRY RETIREMENT AUDIT (attempted + reverted this turn — do NOT re-derive):** Flipping `no-scenario-entry` requires the `personalization` (场景) `getPreferenceNav` surface to stop landing on `ScenarioWorkbench`. Tried: (1) `PersonalizationCenter` landing `'scenario'→'mcp'` + remove the 场景 tab + i18n relabel 场景→扩展 (`personalization.title`, `nav.personalizationDesc`) + acceptance harness `scenes` surface → `extensions` with a `.scenario-workbench === 0` assertion. **Capability is safe to retire** — Research Strategy is a real, reachable authoring surface (`src/research/StrategyEditor.tsx` via Settings → 研究策略) and legacy scenarios auto-project into `strategyList` non-destructively (`engine/personalization/scenarioToStrategy.ts` bound at `createDomains.ts:215`; `ResearchStrategyStore.listStrategies` merges presets+legacy), and it has its own coverage (`StrategyEditor.test.tsx`, `TaskProgressPanel.test.tsx`). **But the blast radius is large and interlocking**, so I reverted to keep §51 green: (a) `tests/frontend/PersonalizationCenter.test.tsx` is a ~54-test suite overwhelmingly mounted on the scenario *landing* (shared `beforeEach`, `selectScenarioInWorkbench`/`runnableScenario` helpers) — 24 tests fail at once; they duplicate `ScenarioWorkbench.test.tsx` (direct render) and would be *deleted*, which must be paired with removing the dead `ScenarioWorkbench` component (900+ lines) + its sub-components/imports/`onActivateScenario` plumbing or lint/typecheck flags them unused; (b) the `scenes` acceptance surface is coupled to the scenario landing — retargeting it to `extensions` fails real-machine `acceptance:layout` unless the app is rebuilt on Electron ABI and re-run; (c) App.test/nav-config/visual-a11y assert the '场景'/'Scenarios' nav label. **Correct order for a future session:** remove the scenario *code* (component + sub-components + dead branch) AND its now-redundant tests in one cascade → regenerate design-system baseline + IPC snapshots if channels/methods move → THEN relabel nav/i18n + flip landing to mcp → THEN retarget the `scenes`→`extensions` harness surface (assert `.scenario-workbench` count 0) → `build:electron` + `acceptance:layout` × 2 → full suite. Until that cascade lands green, §57 "Scenario 用户入口 = 0" stays **TODO**. `getPrimaryResearchNav()` (`src/shell/navConfig.ts`) returns `[]` — topics/outcomes/submissions are no longer top-level nav destinations, realizing "Project 是唯一工作空间". This is genuine convergence, not a proxy: their primary workflows are absorbed into the workbench (outcomes→artifacts list/edit/version/export/trash/compare/promote/submit, topics→§7 confirm, submissions→register+track), and the standalone pages remain reachable via the command palette (`App.tsx` navigate 'standalone') + Global Search as a REVERSIBLE escape hatch (no capability deleted). No router exists so App.tsx research-nav blocks render nothing (structurally safe); ids never persisted. Updated `App.test.tsx` (nav → [personalization,projects,settings]; repurposed the 成果-routing test to assert the destinations are demoted), `navConfig.test.ts` unchanged (vacuous over []), and BOTH python harnesses (`SURFACE_ORDER`/`SURFACE_CONTRACT` in `electron-layout-acceptance.py` + `CURRENT_SURFACE_NAMES` in `run-electron-layout-acceptance.py` → projects+scenes). Verified: app typecheck green, nav tests 38/38, `build:electron` PASS, `acceptance:layout` **status: passed** (2-surface harness), **full suite 6161/0**. `no-office-product-entry` (1 remaining) needs the OutcomesPage external-editor/"Metis Office 原生编辑" flow stripped (§14) — a separate, larger page-internal edit.

**NAV-RETIREMENT PREREQUISITE (audited this turn — do NOT re-derive):** A code audit of removing `topics|outcomes|submissions` from `getPrimaryResearchNav` (`src/shell/navConfig.ts:80`) found: (a) the gate check is a proxy — removing the 3 array entries flips `no-scenario-primary-nav` but the pages REMAIN reachable via the command palette (`src/App.tsx:862-864`) and Global Search (`src/components/GlobalSearch.tsx:32-46`), so deleting only the nav would be gate-gaming, not convergence; (b) real capability is NOT yet absorbed — **F1** OutcomesPage rich Word/PPT/media editing + DOCX/PPTX import/export (Word export now absorbed Batch 31; PPTX + import remain), outcome category tree, trash/restore, mark-final/duplicate/snapshot, source-locate; **F2** TopicWorkspacePage streaming topic-selection conversation (§7 gave confirm only, not exploration); **F3** SubmissionWorkspacePage 选刊 shortlist / submission-assistant chat / portal browser / preflight/freeze/coverLetter tracking (§37 gave registration only); (c) removing nav also breaks `App.test.tsx:99-157`+`:203-209`, `navConfig.test.ts`, and BOTH python harness files' `SURFACE_ORDER`/`CURRENT_SURFACE_NAMES` (they click `[data-nav-id=topics|outcomes]` and hard-fail if absent); `no-office-product-entry` separately needs `src/pages/OutcomesPage.tsx:534-535` `Metis Office 原生编辑` stripped. **Correct order: finish absorbing F1→F3 into the workbench, keep palette/search until parity, THEN delete the nav entries + palette/search paths + update the 2 harness files + the 3 test files, then flip both gates.** Native module left on Node ABI after full-suite runs.
- **Batch 77 (completed — the JSON-half of the parity gate now also covers the strategy store):** the session's earlier B77 entry recorded the `UnifiedArtifactStore` block mid-flight. Completion added the third JSON-record block the audit pointed at: `ResearchStrategyStore.saveStrategy` serializes the whole object but the read path re-parses through `ResearchStrategySchema`, so a TS-record field missing from the zod contract is written happily and read back gone (#22's mechanism with the column question removed). The block saves a full strategy (version 3, two phases with a prompt) and requires save → `getStrategy` → `listStrategies` (the picker's actual view — #19's shape) to deep-equal it, plus a coverage test accounting for every `ResearchStrategySchema` key. RED-proved: mutating the writer to `JSON.stringify({...strategy, version: 1})` fails the block ("survives save → get unchanged"); revert (empty diff on the store) restores **14/14**. `TaskPlanStore` stays out of this gate by design — it round-trips through the human-readable `RESEARCH_PLAN.md` format and has its own 28-test parse/render suite.

- **Batch 78 (§57's last unwired stop condition — PPT from a workbench deliverable — DONE + LIVE):** the product could export a deliverable to Word (B31) but had no path from a 交付成果 to a .pptx: the GordenPPTSkill chain existed only behind the retired Office ribbon. Wired end to end with no new capability invented: new IPC `gordenPpt:buildBriefToDisk` (`registerOutcomeGenerationIpc.ts`, same request contract as `buildFromBrief`) runs `ensureSkillPackage` → `buildFromBrief` → **re-validates the Grid document through `PptDocumentSchema`** (a template output that fails the contract is refused, not saved) → native save dialog (or the `METIS_E2E_EXPORT_DIR` seam shared with the DOCX exports) → real `OutcomePptxService.exportFile`. Preload gains `gordenPptBuildBriefToDisk`; `ArtifactsPanel` gains a「按模板生成 PPT」toggle beside「导出为 Word」on text-bearing artifacts, with a template `<select>` fed by the real `gordenPptListTemplates`, a title defaulted to the artifact title, and 要点 defaulted to the artifact's own markdown list lines (`src/research/briefPoints.ts` — extraction only; a file with no list lines falls back to prose lines; never invented content). Failures surface verbatim (skill download, unusable template, python/pptx missing, write failure, dialog cancel).
  - **Verified at every layer:** handler 6/6 (`tests/electron/GordenPptBriefToDiskIpc.test.ts` — happy path asserts **real PK-zip bytes on disk** written through the real exporter; seam skips the dialog; build-failure/invalid-document/cancel/invalid-request each honest); panel 40/40 (+6: real template list, defaults from the artifact, real fileName surfaced, skill-package failure blocks the form, failed build surfaces python-pptx message, images excluded); `briefPointsFromMarkdown` unit-tested; IPC inventory + 591-method bridge snapshot regenerated (`ipc:snapshot:check` no drift); product gate **13/13** with the new `ppt-deliverable-available` check, mutation-verified (rename the channel → FAIL → revert → GREEN); typecheck(4) 0; lint-gate clean.
  - **LIVE machine run (evidence in the transcript + `logs/gorden-live-deck.pptx`):** real `git clone` of GordenPPTSkill (`cloned: true`), **21 templates** enumerated, real python-pptx 1.0.2 build on `architecture-deck` → `selectedSlides [1,2,3]`, **154 slot edits**, export **11,748 bytes, PK magic**, and the deck's `ppt/slides/slide{1,2,3}.xml` each contain the brief's own points (「平台进入如何改变村庄信息结构」「信任机制在数字治理中的传导路径」「县域案例」). The honest warnings (组合形状/备注/母版 not mapped to the Grid) are import-limitation notes from the real converter, not failures.
  - **Load-flake root cause fixed at the config level:** three full-suite runs on 2026-09-25 (00:46, 01:42, 02:41) produced **3 → 2 → 12 rotating frontend failures** (`App.test` global-search, `OutcomesPage` PPT-bridge, etc.) — every one a jsdom render timing out at the 5s default, every file green in isolation (the two named files re-ran 102/102). Diagnosis: host scheduling pressure, not product regressions — the same class B49 profiled. Fix: `testTimeout: 30_000` on the jsdom vitest project (the node project already carried 30s with the same rationale). Result: full suite **6285 passed / 0 failed / 9 skipped (6294), 589 files, 783 s** — deterministic, not lucky.
  - **Full delivery ladder re-run on the final tree (§51/§55/§56):** typecheck(4) 0 · lint-gate clean · full vitest 6285/0 · pytest **12/12** · `build:electron` PASS → **sweep A 7/7 + sweep B 7/7** (`logs/phase2-sweep-{A,B}/`) → `pack` exit 0 → **packaged CDP smoke 35/35, SMOKE_EXIT=0** → `rebuild:node` → **tier-1 18/18**. Git hygiene note for the push: the local pack is 3.29 GiB but **reachable history is only 0.20 GiB with no blob over 20 MB** (the rest is unreachable orphaned objects — measured via `cat-file --batch-check` over `rev-list --objects --all`), so the full 162-commit history is safe to publish after `git gc`; `logs/` is gitignored so the live deck stays local.

- **Batch 79 (刘总指令：把 D:\插件创建器 —— Agent Capability Studio (ACS) —— 合并进 METIS，且其 Agent Runtime 底座换成 METIS 自己的，不引入 Pi):**
  - **旧创建器彻底移除（刘总拍板）**: `SkillStudioPanel.tsx`（Dialog/Dock/Body）、`engine/skills/SkillStudio.ts`、`skillStudio:generate/testRun` 两个 IPC handler 与 preload 桥、`tests/electron/SkillStudio.test.ts` 全部删除；`registerSkillIpc` 的死引用（zod/runEphemeralChatTurn/trackEphemeralOperation/runtimeShutdown/agentLoop）清理干净。技能页入口按钮改为「打开能力包工作室」（`acsOpenStudio`），技能卡「对话优化」→「在工作室优化」。`skill:generateFromConversation`（B54 的对话生成技能链）不受影响。
  - **ACS 引擎收编（engine/acs/）**: 7 个纯逻辑包 vendor 进 `engine/acs/{ir,core,creator,compiler,installer,testing,community}`（`@acs/*` import 全部重写为相对路径）；`@acs/runtime` **不 vendor 而是重写**——`SessionService`（StudioSession：组装 Store 历史 → METIS `AgentLoop.run`（真实 OpenAICompatProvider，由宿主注入的 `currentConfig` 驱动）→ 结果写回 + 按 ACS web 已消费的事件形状广播 user-message/assistant-message/done/error）、`LoopRunner/GoalRunner`（工作轮走完整 agent 循环，模型验证器改为宿主注入的一次性 `askModel`——不进会话历史不触发工具；迭代/超时/无进展停止条件与原版逐条一致）。**Pi SDK（443MB 依赖）完全没有进入 METIS**。
  - **studio server 工厂化**: 原 `apps/server` 842 行改造为 `engine/acs/studio/server.ts` 的 `createStudioApp(options)` 工厂——40+ REST 端点 + WS 聊天传输原样保留；provider 端点改为 **METIS 桥只读**（GET 返回桥接 profile，POST/DELETE 409 + 明确文案"模型服务由 METIS 统一管理"）；`/api/models` 走 METIS 桥；examples 目录参数化（缺省诚实空表，不再启动失败）；静态 web UI 目录注入。
  - **主进程宿主**: `electron/AcsStudioService`（懒启动：首次 acs:open 才创建 server，listen(0) 内核分配回环端口；单例独立 BrowserWindow（sandbox、无 METIS preload，与产品 renderer 能力面严格隔离）；`drainDomainIpcDisposers` 关停时 `stop()` 全量回收）+ `registerAcsIpc`（acs:open/acs:status，未授权 frame 拒绝）。ACS web 源码 vendor 进 `web-studio/`，`build:electron` 链新增 vite 构建步（→ dist-electron/acs-web）。
  - **zod3→zod4 / 严格度适配**: ACS 源码在 METIS 严格度下收敛——`z.record` 双参、`.default`→`.prefault`（48 处，语义等价：带内层 default 的对象输入默认值）、参数属性改显式赋值、noUncheckedIndexedAccess 守卫。
  - **验证**: typecheck(4) 0 · lint-gate PASS（baseline 204 = engine/acs 128 + web-studio 76 全部 vendor 存量；**B79 新代码 0 违规**）· 产品门禁新增 `capability-studio-available` → **14/14**（变异验证：替换 `new AgentLoop` → FAIL → 还原 → GREEN）· 引擎集成测试 11/11（真 HTTP server + 真 SessionService：无 provider 时诚实 error 而非伪造回答、真 SSE loopback provider 下会话持久化真实回答、LoopRunner 模型验证器 PASS 路径）· IPC 门测 4/4 · 前端 99/99。
  - **交付阶梯（2026-09-25，B79 最终树）**: §55 sweep A + B 各 **7/7** · `pack` exit 0 → 打包 CDP 冒烟 **35/35 PASS**（ACS web 产物随 asar 分发，打包态静态托管正常）· `rebuild:node` → **tier-1 18/18** · 全量 vitest **6296 passed / 0 failed / 9 skipped（6305），590 文件**（6285 + 11 引擎集成 + 4 IPC 门测 − 4 旧 SkillStudio 已删 + 前端改写净变化；skip 恒为 9，无删除换绿）· pytest **12/12**。
  - **真机探针 PASS（`scripts/verify-acs-studio.cjs`）**: 真实 METIS app → 渲染层 `window.metis.acsOpenStudio()` → studio server 于回环随机端口监听（health 200）→ **独立窗口真实渲染 ACS UI**（"你想创建什么 Agent 能力？…"）。中途抓到并修复一个真实缺陷：webDistDir 相对路径错一位（dist-electron/electron/acs-web vs dist-electron/acs-web）导致 "Cannot GET /"——由探针的页面态诊断暴露。
